/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/main.ts
var main_exports = {};
__export(main_exports, {
  default: () => MonitoringPlugin
});
module.exports = __toCommonJS(main_exports);
var import_obsidian18 = require("obsidian");

// src/llm/LLMService.ts
var import_obsidian = require("obsidian");
var LLMService = class {
  constructor(settings) {
    this.settings = settings;
  }
  updateSettings(settings) {
    this.settings = settings;
  }
  async summarizeIncident(content) {
    var _a, _b, _c;
    if (this.settings.useMockLLM) {
      return `**[MOCK] \u041E\u043F\u0438\u0441\u0430\u043D\u0438\u0435 \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u0430:**
\u041F\u0440\u043E\u0438\u0437\u043E\u0448\u043B\u0430 \u0442\u0435\u0441\u0442\u043E\u0432\u0430\u044F \u043E\u0448\u0438\u0431\u043A\u0430. \u0421\u0438\u0441\u0442\u0435\u043C\u044B \u0440\u0430\u0431\u043E\u0442\u0430\u044E\u0442 \u0432 \u0448\u0442\u0430\u0442\u043D\u043E\u043C \u0440\u0435\u0436\u0438\u043C\u0435.

**\u0417\u0430\u0434\u0430\u0447\u0438:**
- \u041F\u0440\u043E\u0432\u0435\u0440\u0438\u0442\u044C \u043B\u043E\u0433\u0438 \u0441\u0435\u0440\u0432\u0435\u0440\u0430
- \u0421\u043E\u043E\u0431\u0449\u0438\u0442\u044C \u0434\u0435\u0436\u0443\u0440\u043D\u043E\u043C\u0443 \u0430\u0434\u043C\u0438\u043D\u0438\u0441\u0442\u0440\u0430\u0442\u043E\u0440\u0443

*\u041E\u0440\u0438\u0433\u0438\u043D\u0430\u043B\u044C\u043D\u044B\u0439 \u0442\u0435\u043A\u0441\u0442:* ${content.slice(0, 50)}...`;
    }
    if (!this.settings.llmApiKey || !this.settings.llmBaseUrl) {
      throw new Error("LLM API Not Configured in Settings!");
    }
    let baseUrl = this.settings.llmBaseUrl.replace(/\/$/, "");
    let endpoint = baseUrl;
    if (!baseUrl.endsWith("/chat/completions")) {
      if (baseUrl.match(/\/(v\d+|api\/v\d+)$/)) {
        endpoint = `${baseUrl}/chat/completions`;
      } else {
        endpoint = `${baseUrl}/v1/chat/completions`;
      }
    }
    const requestData = {
      model: this.settings.llmModel || "llm-medium-moe-instruct",
      messages: [
        { role: "system", content: "\u0422\u044B \u043A\u043E\u0440\u043F\u043E\u0440\u0430\u0442\u0438\u0432\u043D\u044B\u0439 \u0418\u0418-\u0430\u0441\u0441\u0438\u0441\u0442\u0435\u043D\u0442 \u0434\u043B\u044F \u043C\u043E\u043D\u0438\u0442\u043E\u0440\u0438\u043D\u0433\u0430 \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u043E\u0432. \u041E\u0442\u0432\u0435\u0447\u0430\u0439 \u0432\u0441\u0435\u0433\u0434\u0430 \u043D\u0430 \u0440\u0443\u0441\u0441\u043A\u043E\u043C \u044F\u0437\u044B\u043A\u0435." },
        { role: "user", content: `\u0421\u0434\u0435\u043B\u0430\u0439 \u0441\u0430\u043C\u043C\u0430\u0440\u0438 (\u043A\u0440\u0430\u0442\u043A\u0443\u044E \u0432\u044B\u0436\u0438\u043C\u043A\u0443) \u0441\u043B\u0435\u0434\u0443\u044E\u0449\u0435\u0433\u043E \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u0430 \u0438 \u0432\u044B\u0434\u0435\u043B\u0438 \u0437\u0430\u0434\u0430\u0447\u0438:

${content}` }
      ],
      stream: false,
      temperature: 0.7,
      max_tokens: 1500
    };
    const requestParams = {
      url: endpoint,
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${this.settings.llmApiKey}`
      },
      body: JSON.stringify(requestData)
    };
    try {
      const response = await (0, import_obsidian.requestUrl)(requestParams);
      if (response.status >= 200 && response.status < 300) {
        const json = response.json;
        return ((_c = (_b = (_a = json.choices) == null ? void 0 : _a[0]) == null ? void 0 : _b.message) == null ? void 0 : _c.content) || "No summary generated.";
      } else {
        throw new Error(`LLM Error ${response.status}: ${response.text}`);
      }
    } catch (error) {
      console.error("LLM API Error:", error);
      throw new Error(`Failed to contact LLM: ${error.message}`);
    }
  }
  async updateIncidentSummary(oldSummary, newContent) {
    var _a, _b, _c;
    if (this.settings.useMockLLM) {
      return `**[MOCK] \u041E\u0431\u043D\u043E\u0432\u043B\u0435\u043D\u043D\u0430\u044F \u0438\u043D\u0444\u043E\u0440\u043C\u0430\u0446\u0438\u044F \u043F\u043E \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u0443:**

${oldSummary}

**\u041D\u043E\u0432\u043E\u0435 \u043E\u0431\u043D\u043E\u0432\u043B\u0435\u043D\u0438\u0435:**
\u041F\u043E\u043B\u0443\u0447\u0435\u043D\u044B \u043D\u043E\u0432\u044B\u0435 \u0434\u0430\u043D\u043D\u044B\u0435: ${newContent.slice(0, 50)}... \u0422\u0440\u0435\u0431\u0443\u0435\u0442\u0441\u044F \u0430\u043D\u0430\u043B\u0438\u0437.`;
    }
    if (!this.settings.llmApiKey || !this.settings.llmBaseUrl) {
      throw new Error("LLM API Not Configured in Settings!");
    }
    let baseUrl = this.settings.llmBaseUrl.replace(/\/$/, "");
    let endpoint = baseUrl;
    if (!baseUrl.endsWith("/chat/completions")) {
      if (baseUrl.match(/\/(v\d+|api\/v\d+)$/)) {
        endpoint = `${baseUrl}/chat/completions`;
      } else {
        endpoint = `${baseUrl}/v1/chat/completions`;
      }
    }
    const requestData = {
      model: this.settings.llmModel || "llm-medium-moe-instruct",
      messages: [
        { role: "system", content: "\u0422\u044B \u043A\u043E\u0440\u043F\u043E\u0440\u0430\u0442\u0438\u0432\u043D\u044B\u0439 \u0418\u0418-\u0430\u0441\u0441\u0438\u0441\u0442\u0435\u043D\u0442 \u0434\u043B\u044F \u043C\u043E\u043D\u0438\u0442\u043E\u0440\u0438\u043D\u0433\u0430 \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u043E\u0432. \u041E\u0442\u0432\u0435\u0447\u0430\u0439 \u0432\u0441\u0435\u0433\u0434\u0430 \u043D\u0430 \u0440\u0443\u0441\u0441\u043A\u043E\u043C \u044F\u0437\u044B\u043A\u0435." },
        { role: "user", content: `\u0412\u043E\u0442 \u0442\u0435\u043A\u0443\u0449\u0435\u0435 \u0441\u0430\u043C\u043C\u0430\u0440\u0438 \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u0430:
${oldSummary}

\u041F\u043E\u0441\u0442\u0443\u043F\u0438\u043B\u043E \u043D\u043E\u0432\u043E\u0435 \u0441\u043E\u043E\u0431\u0449\u0435\u043D\u0438\u0435 \u043F\u043E \u044D\u0442\u043E\u0439 \u043F\u0440\u043E\u0431\u043B\u0435\u043C\u0435:
${newContent}

\u041F\u043E\u0436\u0430\u043B\u0443\u0439\u0441\u0442\u0430, \u043E\u0431\u043D\u043E\u0432\u0438 \u0441\u0430\u043C\u043C\u0430\u0440\u0438 \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u0430 \u0441 \u0443\u0447\u0435\u0442\u043E\u043C \u043D\u043E\u0432\u043E\u0439 \u0438\u043D\u0444\u043E\u0440\u043C\u0430\u0446\u0438\u0438 (\u043F\u0438\u0448\u0438 \u043D\u0430 \u0440\u0443\u0441\u0441\u043A\u043E\u043C \u044F\u0437\u044B\u043A\u0435).` }
      ],
      stream: false,
      temperature: 0.7,
      max_tokens: 1500
    };
    const requestParams = {
      url: endpoint,
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${this.settings.llmApiKey}`
      },
      body: JSON.stringify(requestData)
    };
    try {
      const response = await (0, import_obsidian.requestUrl)(requestParams);
      if (response.status >= 200 && response.status < 300) {
        const json = response.json;
        return ((_c = (_b = (_a = json.choices) == null ? void 0 : _a[0]) == null ? void 0 : _b.message) == null ? void 0 : _c.content) || "No summary generated.";
      } else {
        throw new Error(`LLM Error ${response.status}: ${response.text}`);
      }
    } catch (error) {
      console.error("LLM API Error:", error);
      throw new Error(`Failed to contact LLM: ${error.message}`);
    }
  }
  async sendChatMessage(messages) {
    var _a, _b, _c;
    if (this.settings.useMockLLM) {
      return `**[MOCK] \u041E\u0442\u0432\u0435\u0442 \u0418\u0418:**
\u0412\u0430\u0448\u0435 \u0441\u043E\u043E\u0431\u0449\u0435\u043D\u0438\u0435 \u043E\u0442\u043F\u0440\u0430\u0432\u043B\u0435\u043D\u043E, \u043D\u043E \u0432\u043A\u043B\u044E\u0447\u0435\u043D \u0442\u0435\u0441\u0442\u043E\u0432\u044B\u0439 \u0440\u0435\u0436\u0438\u043C LLM.`;
    }
    if (!this.settings.llmApiKey || !this.settings.llmBaseUrl) {
      throw new Error("LLM API Not Configured in Settings!");
    }
    let baseUrl = this.settings.llmBaseUrl.replace(/\/$/, "");
    let endpoint = baseUrl;
    if (!baseUrl.endsWith("/chat/completions")) {
      if (baseUrl.match(/\/(v\d+|api\/v\d+)$/)) {
        endpoint = `${baseUrl}/chat/completions`;
      } else {
        endpoint = `${baseUrl}/v1/chat/completions`;
      }
    }
    const chatMessages = [
      { role: "system", content: this.settings.chatSystemPrompt },
      ...messages
    ];
    const requestData = {
      model: this.settings.llmModel || "llm-medium-moe-instruct",
      messages: chatMessages,
      stream: false,
      temperature: this.settings.chatTemperature || 0.7,
      max_tokens: 1500
    };
    const requestParams = {
      url: endpoint,
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${this.settings.llmApiKey}`
      },
      body: JSON.stringify(requestData)
    };
    try {
      const response = await (0, import_obsidian.requestUrl)(requestParams);
      if (response.status >= 200 && response.status < 300) {
        const json = response.json;
        return ((_c = (_b = (_a = json.choices) == null ? void 0 : _a[0]) == null ? void 0 : _b.message) == null ? void 0 : _c.content) || "\u041D\u0435\u0442 \u043E\u0442\u0432\u0435\u0442\u0430 \u043E\u0442 \u043D\u0435\u0439\u0440\u043E\u0441\u0435\u0442\u0438.";
      } else {
        throw new Error(`LLM Error ${response.status}: ${response.text}`);
      }
    } catch (error) {
      console.error("LLM API Error:", error);
      throw new Error(`\u041E\u0448\u0438\u0431\u043A\u0430 \u043F\u0440\u0438 \u0432\u044B\u0437\u043E\u0432\u0435 LLM: ${error.message}`);
    }
  }
  async generateWeeklyReport(incidentsSummary) {
    var _a, _b, _c;
    if (this.settings.useMockLLM) {
      return `**[MOCK] \u0415\u0436\u0435\u043D\u0435\u0434\u0435\u043B\u044C\u043D\u044B\u0439 \u043E\u0442\u0447\u0435\u0442 \u043F\u043E \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u0430\u043C:**
- \u0412\u0441\u0435\u0433\u043E \u0437\u0430\u0444\u0438\u043A\u0441\u0438\u0440\u043E\u0432\u0430\u043D\u043E \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u043E\u0432: 3
- \u041E\u0441\u043D\u043E\u0432\u043D\u044B\u0435 \u043F\u0440\u043E\u0431\u043B\u0435\u043C\u044B \u0441\u0432\u044F\u0437\u0430\u043D\u044B \u0441 \u0431\u0430\u0437\u043E\u0439 \u0434\u0430\u043D\u043D\u044B\u0445.
- \u0412\u0441\u0435 \u043A\u0440\u0438\u0442\u0438\u0447\u0435\u0441\u043A\u0438\u0435 \u0441\u0431\u043E\u0438 \u0443\u0441\u0442\u0440\u0430\u043D\u0435\u043D\u044B \u0432 \u0442\u0435\u0447\u0435\u043D\u0438\u0435 \u0447\u0430\u0441\u0430.`;
    }
    if (!this.settings.llmApiKey || !this.settings.llmBaseUrl) {
      throw new Error("LLM API Not Configured in Settings!");
    }
    let baseUrl = this.settings.llmBaseUrl.replace(/\/$/, "");
    let endpoint = baseUrl;
    if (!baseUrl.endsWith("/chat/completions")) {
      if (baseUrl.match(/\/(v\d+|api\/v\d+)$/)) {
        endpoint = `${baseUrl}/chat/completions`;
      } else {
        endpoint = `${baseUrl}/v1/chat/completions`;
      }
    }
    const requestData = {
      model: this.settings.llmModel || "llm-medium-moe-instruct",
      messages: [
        { role: "system", content: "\u0422\u044B \u043A\u043E\u0440\u043F\u043E\u0440\u0430\u0442\u0438\u0432\u043D\u044B\u0439 \u0418\u0418-\u0430\u0441\u0441\u0438\u0441\u0442\u0435\u043D\u0442 \u0434\u043B\u044F \u0440\u0443\u043A\u043E\u0432\u043E\u0434\u0438\u0442\u0435\u043B\u0435\u0439. \u0422\u0432\u043E\u044F \u0437\u0430\u0434\u0430\u0447\u0430 \u2014 \u0430\u043D\u0430\u043B\u0438\u0437\u0438\u0440\u043E\u0432\u0430\u0442\u044C \u0441\u043F\u0438\u0441\u043E\u043A \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u043E\u0432 \u0438 \u0441\u043E\u0441\u0442\u0430\u0432\u043B\u044F\u0442\u044C \u043A\u0440\u0430\u0442\u043A\u0438\u0439, \u0441\u0442\u0440\u0443\u043A\u0442\u0443\u0440\u0438\u0440\u043E\u0432\u0430\u043D\u043D\u044B\u0439 \u0435\u0436\u0435\u043D\u0435\u0434\u0435\u043B\u044C\u043D\u044B\u0439 \u0430\u043D\u0430\u043B\u0438\u0442\u0438\u0447\u0435\u0441\u043A\u0438\u0439 \u043E\u0442\u0447\u0435\u0442. \u041E\u0442\u0432\u0435\u0447\u0430\u0439 \u043D\u0430 \u0440\u0443\u0441\u0441\u043A\u043E\u043C \u044F\u0437\u044B\u043A\u0435." },
        { role: "user", content: `\u041D\u0430 \u043E\u0441\u043D\u043E\u0432\u0435 \u0441\u043B\u0435\u0434\u0443\u044E\u0449\u0438\u0445 \u0441\u0430\u043C\u043C\u0430\u0440\u0438 \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u043E\u0432 \u0437\u0430 \u043D\u0435\u0434\u0435\u043B\u044E, \u0441\u043E\u0441\u0442\u0430\u0432\u044C \u0430\u043D\u0430\u043B\u0438\u0442\u0438\u0447\u0435\u0441\u043A\u0438\u0439 \u043E\u0442\u0447\u0435\u0442 \u0434\u043B\u044F \u0440\u0443\u043A\u043E\u0432\u043E\u0434\u0438\u0442\u0435\u043B\u044F. \u0412\u044B\u0434\u0435\u043B\u0438 \u043E\u0441\u043D\u043E\u0432\u043D\u044B\u0435 \u0442\u0440\u0435\u043D\u0434\u044B, \u043A\u0440\u0438\u0442\u0438\u0447\u0435\u0441\u043A\u0438\u0435 \u043F\u0440\u043E\u0431\u043B\u0435\u043C\u044B \u0438 \u043F\u0440\u0435\u0434\u043B\u043E\u0436\u0438 \u0440\u0435\u043A\u043E\u043C\u0435\u043D\u0434\u0430\u0446\u0438\u0438:

${incidentsSummary}` }
      ],
      stream: false,
      temperature: 0.5,
      max_tokens: 2e3
    };
    const requestParams = {
      url: endpoint,
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${this.settings.llmApiKey}`
      },
      body: JSON.stringify(requestData)
    };
    try {
      const response = await (0, import_obsidian.requestUrl)(requestParams);
      if (response.status >= 200 && response.status < 300) {
        const json = response.json;
        return ((_c = (_b = (_a = json.choices) == null ? void 0 : _a[0]) == null ? void 0 : _b.message) == null ? void 0 : _c.content) || "\u041D\u0435 \u0443\u0434\u0430\u043B\u043E\u0441\u044C \u0441\u0433\u0435\u043D\u0435\u0440\u0438\u0440\u043E\u0432\u0430\u0442\u044C \u043E\u0442\u0447\u0435\u0442.";
      } else {
        throw new Error(`LLM Error ${response.status}: ${response.text}`);
      }
    } catch (error) {
      console.error("LLM API Error:", error);
      throw new Error(`\u041E\u0448\u0438\u0431\u043A\u0430 \u043F\u0440\u0438 \u0433\u0435\u043D\u0435\u0440\u0430\u0446\u0438\u0438 \u043E\u0442\u0447\u0435\u0442\u0430: ${error.message}`);
    }
  }
  async checkSpelling(text) {
    var _a, _b, _c;
    if (this.settings.useMockLLM) {
      return `[MOCK] \u0418\u0441\u043F\u0440\u0430\u0432\u043B\u0435\u043D\u043D\u0430\u044F \u043E\u0440\u0444\u043E\u0433\u0440\u0430\u0444\u0438\u044F:
${text}`;
    }
    if (!this.settings.llmApiKey || !this.settings.llmBaseUrl) {
      throw new Error("LLM API Not Configured in Settings!");
    }
    let baseUrl = this.settings.llmBaseUrl.replace(/\/$/, "");
    let endpoint = baseUrl;
    if (!baseUrl.endsWith("/chat/completions")) {
      if (baseUrl.match(/\/(v\d+|api\/v\d+)$/)) {
        endpoint = `${baseUrl}/chat/completions`;
      } else {
        endpoint = `${baseUrl}/v1/chat/completions`;
      }
    }
    const requestData = {
      model: this.settings.llmModel || "llm-medium-moe-instruct",
      messages: [
        { role: "system", content: "\u0422\u044B \u043E\u043F\u044B\u0442\u043D\u044B\u0439 \u0440\u0435\u0434\u0430\u043A\u0442\u043E\u0440. \u0418\u0441\u043F\u0440\u0430\u0432\u043B\u044F\u0439 \u043E\u0440\u0444\u043E\u0433\u0440\u0430\u0444\u0438\u0447\u0435\u0441\u043A\u0438\u0435 \u0438 \u0433\u0440\u0430\u043C\u043C\u0430\u0442\u0438\u0447\u0435\u0441\u043A\u0438\u0435 \u043E\u0448\u0438\u0431\u043A\u0438 \u0432 \u0442\u0435\u043A\u0441\u0442\u0435. \u0412\u043E\u0437\u0432\u0440\u0430\u0449\u0430\u0439 \u0442\u043E\u043B\u044C\u043A\u043E \u0438\u0441\u043F\u0440\u0430\u0432\u043B\u0435\u043D\u043D\u044B\u0439 \u0442\u0435\u043A\u0441\u0442 \u0431\u0435\u0437 \u043A\u043E\u043C\u043C\u0435\u043D\u0442\u0430\u0440\u0438\u0435\u0432." },
        { role: "user", content: `\u0418\u0441\u043F\u0440\u0430\u0432\u044C \u043E\u0440\u0444\u043E\u0433\u0440\u0430\u0444\u0438\u044E:
${text}` }
      ],
      stream: false,
      temperature: 0.3,
      max_tokens: 2e3
    };
    const requestParams = {
      url: endpoint,
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${this.settings.llmApiKey}`
      },
      body: JSON.stringify(requestData)
    };
    try {
      const response = await (0, import_obsidian.requestUrl)(requestParams);
      if (response.status >= 200 && response.status < 300) {
        const json = response.json;
        return ((_c = (_b = (_a = json.choices) == null ? void 0 : _a[0]) == null ? void 0 : _b.message) == null ? void 0 : _c.content) || text;
      } else {
        throw new Error(`LLM Error ${response.status}: ${response.text}`);
      }
    } catch (error) {
      console.error("LLM API Error:", error);
      throw error;
    }
  }
  async rephrase(text) {
    var _a, _b, _c;
    if (this.settings.useMockLLM) {
      return `[MOCK] \u041F\u0435\u0440\u0435\u0444\u043E\u0440\u043C\u0443\u043B\u0438\u0440\u043E\u0432\u0430\u043D\u043D\u044B\u0439 \u0442\u0435\u043A\u0441\u0442:
${text}`;
    }
    if (!this.settings.llmApiKey || !this.settings.llmBaseUrl) {
      throw new Error("LLM API Not Configured in Settings!");
    }
    let baseUrl = this.settings.llmBaseUrl.replace(/\/$/, "");
    let endpoint = baseUrl;
    if (!baseUrl.endsWith("/chat/completions")) {
      if (baseUrl.match(/\/(v\d+|api\/v\d+)$/)) {
        endpoint = `${baseUrl}/chat/completions`;
      } else {
        endpoint = `${baseUrl}/v1/chat/completions`;
      }
    }
    const requestData = {
      model: this.settings.llmModel || "llm-medium-moe-instruct",
      messages: [
        { role: "system", content: "\u0422\u044B \u043E\u043F\u044B\u0442\u043D\u044B\u0439 \u0440\u0435\u0434\u0430\u043A\u0442\u043E\u0440. \u041F\u0435\u0440\u0435\u0444\u043E\u0440\u043C\u0443\u043B\u0438\u0440\u0443\u0439 \u0442\u0435\u043A\u0441\u0442 \u0431\u043E\u043B\u0435\u0435 \u0433\u0440\u0430\u043C\u043E\u0442\u043D\u043E, \u0447\u0435\u0442\u043A\u043E \u0438 \u043F\u0440\u043E\u0444\u0435\u0441\u0441\u0438\u043E\u043D\u0430\u043B\u044C\u043D\u043E. \u0421\u043E\u0445\u0440\u0430\u043D\u0438 \u0441\u043C\u044B\u0441\u043B. \u0412\u043E\u0437\u0432\u0440\u0430\u0449\u0430\u0439 \u0442\u043E\u043B\u044C\u043A\u043E \u043F\u0435\u0440\u0435\u0444\u043E\u0440\u043C\u0443\u043B\u0438\u0440\u043E\u0432\u0430\u043D\u043D\u044B\u0439 \u0442\u0435\u043A\u0441\u0442 \u0431\u0435\u0437 \u043A\u043E\u043C\u043C\u0435\u043D\u0442\u0430\u0440\u0438\u0435\u0432." },
        { role: "user", content: `\u041F\u0435\u0440\u0435\u0444\u043E\u0440\u043C\u0443\u043B\u0438\u0440\u0443\u0439:
${text}` }
      ],
      stream: false,
      temperature: 0.5,
      max_tokens: 2e3
    };
    const requestParams = {
      url: endpoint,
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${this.settings.llmApiKey}`
      },
      body: JSON.stringify(requestData)
    };
    try {
      const response = await (0, import_obsidian.requestUrl)(requestParams);
      if (response.status >= 200 && response.status < 300) {
        const json = response.json;
        return ((_c = (_b = (_a = json.choices) == null ? void 0 : _a[0]) == null ? void 0 : _b.message) == null ? void 0 : _c.content) || text;
      } else {
        throw new Error(`LLM Error ${response.status}: ${response.text}`);
      }
    } catch (error) {
      console.error("LLM API Error:", error);
      throw error;
    }
  }
};

// src/outlook/OutlookService.ts
var import_child_process = require("child_process");
var path = __toESM(require("path"));
var OutlookService = class {
  constructor(settings, app) {
    this.settings = settings;
    this.app = app;
  }
  updateSettings(settings) {
    this.settings = settings;
  }
  async fetchEmails() {
    if (this.settings.useMockData) {
      return [
        {
          entryId: `mock-1-${Date.now()}`,
          conversationTopic: "\u041E\u0448\u0438\u0431\u043A\u0430 \u0441\u0435\u0440\u0432\u0435\u0440\u0430 \u0411\u0414",
          subject: "\u041A\u0440\u0438\u0442\u0438\u0447\u0435\u0441\u043A\u0430\u044F \u043E\u0448\u0438\u0431\u043A\u0430: \u0441\u0435\u0440\u0432\u0435\u0440 \u0431\u0430\u0437\u044B \u0434\u0430\u043D\u043D\u044B\u0445 \u043D\u0435 \u043E\u0442\u0432\u0435\u0447\u0430\u0435\u0442",
          sender: "\u0421\u0438\u0441\u0442\u0435\u043C\u0430 \u041C\u043E\u043D\u0438\u0442\u043E\u0440\u0438\u043D\u0433\u0430 (Zabbix)",
          receivedDateTime: new Date(Date.now() - 36e5).toISOString(),
          bodyPreview: "\u0417\u0434\u0440\u0430\u0432\u0441\u0442\u0432\u0443\u0439\u0442\u0435!\n\n\u0410\u0432\u0442\u043E\u043C\u0430\u0442\u0438\u0447\u0435\u0441\u043A\u0438\u0439 \u043C\u043E\u043D\u0438\u0442\u043E\u0440\u0438\u043D\u0433 \u0437\u0430\u0444\u0438\u043A\u0441\u0438\u0440\u043E\u0432\u0430\u043B \u043D\u0435\u0434\u043E\u0441\u0442\u0443\u043F\u043D\u043E\u0441\u0442\u044C \u0441\u0435\u0440\u0432\u0435\u0440\u0430 DB-MAIN-01. \u0412\u0440\u0435\u043C\u044F \u043E\u0442\u043A\u043B\u0438\u043A\u0430 \u043F\u0440\u0435\u0432\u044B\u0448\u0430\u0435\u0442 60000\u043C\u0441. \n\n\u0421\u043B\u0443\u0436\u0431\u0430 \u0431\u044B\u043B\u0430 \u043F\u0435\u0440\u0435\u0437\u0430\u043F\u0443\u0449\u0435\u043D\u0430, \u043D\u043E \u043F\u0440\u043E\u0431\u043B\u0435\u043C\u0430 \u0441\u043E\u0445\u0440\u0430\u043D\u044F\u0435\u0442\u0441\u044F. \u0422\u0440\u0435\u0431\u0443\u0435\u0442\u0441\u044F \u0432\u043C\u0435\u0448\u0430\u0442\u0435\u043B\u044C\u0441\u0442\u0432\u043E \u0438\u043D\u0436\u0435\u043D\u0435\u0440\u0430.\n"
        },
        {
          entryId: `mock-2-${Date.now()}`,
          conversationTopic: "\u041E\u0448\u0438\u0431\u043A\u0430 \u0441\u0435\u0440\u0432\u0435\u0440\u0430 \u0411\u0414",
          subject: "Re: \u041A\u0440\u0438\u0442\u0438\u0447\u0435\u0441\u043A\u0430\u044F \u043E\u0448\u0438\u0431\u043A\u0430: \u0441\u0435\u0440\u0432\u0435\u0440 \u0431\u0430\u0437\u044B \u0434\u0430\u043D\u043D\u044B\u0445 \u043D\u0435 \u043E\u0442\u0432\u0435\u0447\u0430\u0435\u0442",
          sender: "\u0418\u0432\u0430\u043D \u0418\u0432\u0430\u043D\u043E\u0432 (DevOps)",
          receivedDateTime: new Date().toISOString(),
          bodyPreview: "\u0412\u0438\u0436\u0443 \u043F\u0440\u043E\u0431\u043B\u0435\u043C\u0443. \u0417\u0430\u0448\u0435\u043B \u043D\u0430 \u0441\u0435\u0440\u0432\u0435\u0440, \u043B\u043E\u0433 \u0437\u0430\u0431\u0438\u0442 \u0437\u0430\u043F\u0440\u043E\u0441\u0430\u043C\u0438 \u043E\u0442 \u043D\u043E\u0432\u043E\u0433\u043E \u043C\u0438\u043A\u0440\u043E\u0441\u0435\u0440\u0432\u0438\u0441\u0430 \u0430\u043D\u0430\u043B\u0438\u0442\u0438\u043A\u0438. \u0421\u0435\u0439\u0447\u0430\u0441 \u0432\u0440\u0435\u043C\u0435\u043D\u043D\u043E \u043B\u0438\u043C\u0438\u0442\u0438\u0440\u0443\u044E \u0438\u0445 \u043F\u0443\u043B \u0441\u043E\u0435\u0434\u0438\u043D\u0435\u043D\u0438\u0439, \u0447\u0442\u043E\u0431\u044B \u043E\u0441\u0432\u043E\u0431\u043E\u0434\u0438\u0442\u044C \u0440\u0435\u0441\u0443\u0440\u0441\u044B."
        },
        {
          entryId: `mock-3-${Date.now()}`,
          conversationTopic: "\u041D\u043E\u0432\u044B\u0439 \u0440\u0435\u043B\u0438\u0437 frontend",
          subject: "\u0420\u0435\u043B\u0438\u0437 1.5.0 \u0443\u0441\u043F\u0435\u0448\u043D\u043E \u0440\u0430\u0437\u0432\u0435\u0440\u043D\u0443\u0442",
          sender: "CI/CD Pipeline",
          receivedDateTime: new Date().toISOString(),
          bodyPreview: "\u0414\u0435\u043F\u043B\u043E\u0439 \u0437\u0430\u0432\u0435\u0440\u0448\u0435\u043D \u0431\u0435\u0437 \u043E\u0448\u0438\u0431\u043E\u043A. \u0412\u0441\u0435 \u043F\u043E\u0434\u044B \u043F\u043E\u0434\u043D\u044F\u043B\u0438\u0441\u044C \u0443\u0441\u043F\u0435\u0448\u043D\u043E."
        }
      ];
    }
    return new Promise((resolve, reject) => {
      const adapter = this.app.vault.adapter;
      const basePath = adapter.getBasePath();
      const pythonScriptPath = path.join(basePath, ".obsidian", "plugins", "monitoring-plugin", "outlook", "fetch_mail.py");
      const pythonCommand = `chcp 65001 >nul && python "${pythonScriptPath}" --folder "${this.settings.mailFolder}"`;
      (0, import_child_process.exec)(pythonCommand, {
        encoding: "utf-8",
        env: { ...process.env, PYTHONIOENCODING: "utf-8" }
      }, (error, stdout, stderr) => {
        if (error) {
          console.error("Exec error:", error);
          return reject(new Error("Failed to execute Outlook python script. Ensure Python is installed and win32com is available."));
        }
        if (stderr) {
          console.warn("Python stderr:", stderr);
        }
        try {
          const result = JSON.parse(stdout);
          resolve(result.value || []);
        } catch (parseError) {
          console.error("JSON Parse error:", parseError, stdout);
          reject(new Error("Invalid response format from Python script."));
        }
      });
    });
  }
};

// src/notes/TemplateManager.ts
var TemplateManager = class {
  constructor(app, settings) {
    this.app = app;
    this.settings = settings;
  }
  updateSettings(settings) {
    this.settings = settings;
  }
  async getIncidentNoteByTopic(topic) {
    const files = this.app.vault.getMarkdownFiles();
    for (const file of files) {
      const cache = this.app.metadataCache.getFileCache(file);
      if ((cache == null ? void 0 : cache.frontmatter) && cache.frontmatter["conversation_topic"] === topic) {
        return file;
      }
    }
    return null;
  }
  async createIncidentNote(email, summary) {
    var _a;
    const vault = this.app.vault;
    const topic = email.conversationTopic || email.subject || "Unknown";
    const safeTitle = topic.replace(/[\\/:"*?<>|]/g, "_").slice(0, 50);
    let targetFolder = "";
    let folderPath = (_a = this.settings.incidentsFolder) == null ? void 0 : _a.trim();
    if (folderPath) {
      folderPath = folderPath.replace(/\\/g, "/").replace(/^\/+|\/+$/g, "");
      targetFolder = folderPath + "/";
      const folderAbstract = vault.getAbstractFileByPath(folderPath);
      if (!folderAbstract) {
        try {
          let currentPath = "";
          const parts = folderPath.split("/");
          for (const part of parts) {
            currentPath = currentPath === "" ? part : `${currentPath}/${part}`;
            let folderExists = vault.getAbstractFileByPath(currentPath);
            if (!folderExists) {
              await vault.createFolder(currentPath);
            }
          }
        } catch (e) {
          console.error("Failed to create folder", e);
        }
      }
    }
    const filename = `${targetFolder}Incident-${safeTitle}.md`;
    const fileContent = `---
date: ${email.receivedDateTime}
sender: "${email.sender}"
subject: "${email.subject}"
conversation_topic: "${email.conversationTopic}"
status: "Pending"
tags: [incident]
cssclasses: [hide-properties]
---

# ${email.conversationTopic}

## \u0422\u0435\u043A\u0443\u0449\u0435\u0435 \u0441\u0430\u043C\u043C\u0430\u0440\u0438 \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u0430
${summary}

---

## \u041B\u043E\u0433 \u0441\u043E\u043E\u0431\u0449\u0435\u043D\u0438\u0439

**\u041E\u0442 \u043A\u043E\u0433\u043E:** ${email.sender}
**\u0414\u0430\u0442\u0430:** ${email.receivedDateTime}
**\u0422\u0435\u043C\u0430:** ${email.subject}
**\u0421\u043E\u043E\u0431\u0449\u0435\u043D\u0438\u0435:**
${email.bodyPreview}
`;
    let file = vault.getAbstractFileByPath(filename);
    if (file) {
      await vault.modify(file, fileContent);
      return file;
    } else {
      return vault.create(filename, fileContent);
    }
  }
  async updateIncidentNote(file, email, newSummary) {
    let content = await this.app.vault.read(file);
    const summaryRegex = /## Текущее саммари инцидента\n([\s\S]*?)\n---/m;
    if (summaryRegex.test(content)) {
      content = content.replace(summaryRegex, `## \u0422\u0435\u043A\u0443\u0449\u0435\u0435 \u0441\u0430\u043C\u043C\u0430\u0440\u0438 \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u0430
${newSummary}

---`);
    } else {
      content += `

## \u041D\u043E\u0432\u043E\u0435 \u0441\u0430\u043C\u043C\u0430\u0440\u0438:
${newSummary}`;
    }
    const newLogEntry = `
**\u041E\u0442 \u043A\u043E\u0433\u043E:** ${email.sender}
**\u0414\u0430\u0442\u0430:** ${email.receivedDateTime}
**\u0422\u0435\u043C\u0430:** ${email.subject}
**\u0421\u043E\u043E\u0431\u0449\u0435\u043D\u0438\u0435:**
${email.bodyPreview}
---`;
    content += newLogEntry;
    await this.app.vault.modify(file, content);
  }
  async createTaskNote(name, initialTags = [], linkedProject = "") {
    const vault = this.app.vault;
    const folder = "tasks";
    await this.ensureFolder(folder);
    const dateStr = new Date().toISOString().split("T")[0];
    const fileName = `${folder}/Task-${name.replace(/[\\/:"*?<>|]/g, "_")}.md`;
    const tags = [.../* @__PURE__ */ new Set(["task", ...initialTags])];
    const tagsStr = tags.length > 1 ? `[${tags.join(", ")}]` : tags[0];
    const content = `---
type: task
status: "To Do"
priority: 3
created: ${dateStr}
deadline: ${dateStr} 10:00
linked_project: "${linkedProject}"
tags: ${tagsStr}
responsible: "${this.settings.currentUser}"
cssclasses: [hide-properties]
---

# ${name}

\`\`\`monitoring-duration
\`\`\`

## \u{1F4CB} \u041E\u043F\u0438\u0441\u0430\u043D\u0438\u0435
\u0417\u0430\u043F\u0438\u0448\u0438\u0442\u0435 \u0437\u0434\u0435\u0441\u044C \u0434\u0435\u0442\u0430\u043B\u0438 \u0437\u0430\u0434\u0430\u0447\u0438...

## \u2705 \u0427\u0435\u043A-\u043B\u0438\u0441\u0442
- [ ] 

## \u{1F4DD} \u0417\u0430\u043C\u0435\u0442\u043A\u0438
...
`;
    return vault.create(fileName, content);
  }
  async createProjectNote(name) {
    const vault = this.app.vault;
    const folder = "projects";
    await this.ensureFolder(folder);
    const dateStr = new Date().toISOString().split("T")[0];
    const fileName = `${folder}/Project-${name.replace(/[\\/:"*?<>|]/g, "_")}.md`;
    const codeBlock = "```monitoring-duration\n```";
    const content = `---
type: project
status: Active
started: ${dateStr}
target_date: 
owner: "${this.app.vault.getName()}"
goal: ""
tags: [project]
responsible: "${this.settings.currentUser}"
tracked_emails: []
cssclasses: [hide-properties]
---

# ${name}

${codeBlock}

## \u{1F3AF} \u0426\u0435\u043B\u0438 \u043F\u0440\u043E\u0435\u043A\u0442\u0430
1. 

## \u{1F3D7} \u042D\u0442\u0430\u043F\u044B (Milestones)
- [ ] \u0418\u043D\u0438\u0446\u0438\u0430\u043B\u0438\u0437\u0430\u0446\u0438\u044F
- [ ] \u0420\u0430\u0437\u0440\u0430\u0431\u043E\u0442\u043A\u0430
- [ ] \u0422\u0435\u0441\u0442\u0438\u0440\u043E\u0432\u0430\u043D\u0438\u0435
- [ ] \u0417\u0430\u043F\u0443\u0441\u043A

## \u{1F4EC} \u041F\u0438\u0441\u044C\u043C\u0430

`;
    return vault.create(fileName, content);
  }
  async createSimpleNote(name) {
    const vault = this.app.vault;
    const folder = this.settings.simpleNotesFolder || "notes";
    await this.ensureFolder(folder);
    const dateStr = new Date().toISOString().split("T")[0];
    const fileName = `${folder}/${name.replace(/[\\/:"*?<>|]/g, "_")}.md`;
    const content = `---
type: note
created: ${dateStr}
author: "${this.settings.currentUser}"
tags: [note]
cssclasses: [hide-properties]
---

# ${name}

\`\`\`monitoring-duration
\`\`\`

## \u{1F4DD} \u0422\u0435\u043A\u0441\u0442 \u0437\u0430\u043C\u0435\u0442\u043A\u0438
\u041D\u0430\u0447\u043D\u0438\u0442\u0435 \u043F\u0438\u0441\u0430\u0442\u044C \u0437\u0434\u0435\u0441\u044C...

`;
    return vault.create(fileName, content);
  }
  async ensureFolder(path2) {
    const folder = this.app.vault.getAbstractFileByPath(path2);
    if (!folder) {
      await this.app.vault.createFolder(path2);
    }
  }
  async readNoteContent(file) {
    return await this.app.vault.read(file);
  }
  async updateSubtaskTable(parentFile, taskFile) {
    let content = await this.app.vault.read(parentFile);
    const taskContent = await this.app.vault.read(taskFile);
    const statusMatch = taskContent.match(/^status:\s*["']?(.+?)["']?\s*$/m);
    const deadlineMatch = taskContent.match(/^deadline:\s*["']?(.+?)["']?\s*$/m);
    const status = statusMatch ? statusMatch[1].trim() : "To Do";
    const deadline = deadlineMatch ? deadlineMatch[1].trim() : "\u041D\u0435 \u0437\u0430\u0434\u0430\u043D\u043E";
    const statusIcon = this.getStatusIcon(status);
    const header = "## \u{1F4CB} \u0421\u043F\u0438\u0441\u043E\u043A \u043F\u043E\u0434\u0437\u0430\u0434\u0430\u0447";
    const listItem = `- ${statusIcon} [[${taskFile.basename}]] | ${status} | ${deadline}`;
    if (content.includes(header)) {
      content = content.replace(header, `${header}
${listItem}`);
    } else {
      const logHeader = "## \u041B\u043E\u0433 \u0441\u043E\u043E\u0431\u0449\u0435\u043D\u0438\u0439";
      const section = `
${header}
${listItem}
`;
      if (content.includes(logHeader)) {
        content = content.replace(logHeader, `${section}${logHeader}`);
      } else {
        content += section;
      }
    }
    await this.app.vault.modify(parentFile, content);
  }
  async updateSubtaskStatusIcon(taskFile) {
    var _a;
    const files = this.app.vault.getMarkdownFiles();
    const taskBasename = taskFile.basename;
    const cache = this.app.metadataCache.getFileCache(taskFile);
    const newStatus = ((_a = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _a["status"]) || "To Do";
    const newIcon = this.getStatusIcon(newStatus);
    for (const file of files) {
      if (file.path === taskFile.path)
        continue;
      const content = await this.app.vault.read(file);
      if (content.includes(`[[${taskBasename}]]`)) {
        const regex = new RegExp(`(- [\u2705\u{1F504}\u2B1C] \\[\\[${taskBasename}\\]\\][^\\n]*)`);
        const match = content.match(regex);
        if (match) {
          const oldLine = match[1];
          const newLine = oldLine.replace(/^- [✅🔄⬜]/, `- ${newIcon}`);
          const updatedContent = content.replace(oldLine, newLine);
          await this.app.vault.modify(file, updatedContent);
        }
      }
    }
  }
  getStatusIcon(status) {
    const s = status.toLowerCase();
    if (s.includes("\u0437\u0430\u0432\u0435\u0440\u0448\u0435\u043D") || s.includes("\u0432\u044B\u043F\u043E\u043B\u043D\u0435\u043D\u043E") || s === "done" || s === "completed")
      return "\u2705";
    if (s.includes("\u0432 \u0440\u0430\u0431\u043E\u0442\u0435") || s.includes("\u043F\u0440\u043E\u0446\u0435\u0441\u0441\u0435") || s === "active" || s === "in progress")
      return "\u{1F504}";
    return "\u2B1C";
  }
};

// src/daily/DailyService.ts
var import_obsidian2 = require("obsidian");
var DailyService = class {
  constructor(app, settings) {
    this.app = app;
    this.settings = settings;
  }
  getDayKey(date) {
    const days = {
      0: "sun",
      1: "mon",
      2: "tue",
      3: "wed",
      4: "thu",
      5: "fri",
      6: "sat"
    };
    return days[date.getDay()];
  }
  getDayName(date) {
    const days = {
      0: "\u0412\u043E\u0441\u043A\u0440\u0435\u0441\u0435\u043D\u044C\u0435",
      1: "\u041F\u043E\u043D\u0435\u0434\u0435\u043B\u044C\u043D\u0438\u043A",
      2: "\u0412\u0442\u043E\u0440\u043D\u0438\u043A",
      3: "\u0421\u0440\u0435\u0434\u0430",
      4: "\u0427\u0435\u0442\u0432\u0435\u0440\u0433",
      5: "\u041F\u044F\u0442\u043D\u0438\u0446\u0430",
      6: "\u0421\u0443\u0431\u0431\u043E\u0442\u0430"
    };
    return days[date.getDay()];
  }
  async parseRoutines() {
    const routinesFile = this.app.vault.getAbstractFileByPath("routines.md");
    if (!routinesFile || !(routinesFile instanceof import_obsidian2.TFile)) {
      return this.getDefaultRoutines();
    }
    try {
      const content = await this.app.vault.read(routinesFile);
      return this.parseRoutinesContent(content);
    } catch (e) {
      console.error("Error reading routines.md:", e);
      return this.getDefaultRoutines();
    }
  }
  getDefaultRoutines() {
    return {
      every_day: [
        { name: "\u041F\u0440\u043E\u0432\u0435\u0440\u0438\u0442\u044C \u043F\u043E\u0447\u0442\u0443", time: "09:00" },
        { name: "\u041E\u0431\u0437\u043E\u0440 \u0437\u0430\u0434\u0430\u0447 \u043D\u0430 \u0434\u0435\u043D\u044C", time: "09:30" },
        { name: "\u0410\u043A\u0442\u0443\u0430\u043B\u0438\u0437\u0438\u0440\u043E\u0432\u0430\u0442\u044C \u0441\u0442\u0430\u0442\u0443\u0441\u044B \u0437\u0430\u0434\u0430\u0447", time: "17:00" }
      ],
      mon: [],
      tue: [
        { name: "\u041F\u043E\u0434\u0433\u043E\u0442\u043E\u0432\u043A\u0430 \u0435\u0436\u0435\u043D\u0435\u0434\u0435\u043B\u044C\u043D\u043E\u0433\u043E \u043E\u0442\u0447\u0451\u0442\u0430", time: "14:00" }
      ],
      wed: [],
      thu: [],
      fri: [
        { name: "\u0421\u043E\u0431\u0440\u0430\u0442\u044C \u0432\u043E\u043F\u0440\u043E\u0441\u044B \u0434\u043B\u044F \u0432\u0441\u0442\u0440\u0435\u0447\u0438", time: "14:00" },
        { name: "\u0424\u043E\u0440\u043C\u0438\u0440\u043E\u0432\u0430\u043D\u0438\u0435 \u043F\u043E\u0432\u0435\u0441\u0442\u043A\u0438", time: "15:00" }
      ],
      sat: [],
      sun: []
    };
  }
  parseRoutinesContent(content) {
    const routines = {
      every_day: [],
      mon: [],
      tue: [],
      wed: [],
      thu: [],
      fri: [],
      sat: [],
      sun: []
    };
    const lines = content.split("\n");
    let currentDay = "";
    for (const line of lines) {
      const dayMatch = line.match(/^(\w+):\s*$/);
      if (dayMatch) {
        currentDay = dayMatch[1];
        if (!routines[currentDay]) {
          routines[currentDay] = [];
        }
        continue;
      }
      const taskMatch = line.match(/^\s*-\s*["']?([^"']+)["']?(?:\s*\|\s*(\d{2}:\d{2}))?/);
      if (taskMatch && currentDay) {
        routines[currentDay].push({
          name: taskMatch[1].trim(),
          time: taskMatch[2]
        });
      }
    }
    return routines;
  }
  async getTodayTasks() {
    var _a;
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    const todayEnd = new Date(today);
    todayEnd.setHours(23, 59, 59, 999);
    const tasks = [];
    const files = this.app.vault.getMarkdownFiles();
    for (const file of files) {
      if (!file.basename.startsWith("Task-"))
        continue;
      const cache = this.app.metadataCache.getFileCache(file);
      const deadlineStr = (_a = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _a["deadline"];
      if (!deadlineStr)
        continue;
      if (this.isDeadlineMatch(deadlineStr, today, todayEnd)) {
        tasks.push(file);
      }
    }
    return tasks;
  }
  isDeadlineMatch(deadlineStr, today, todayEnd) {
    if (deadlineStr.includes(" to ")) {
      const [startStr, endStr] = deadlineStr.split(" to ");
      const start = this.parseDate(startStr.trim());
      const end = this.parseDate(endStr.split(" ")[0].trim());
      if (start && end) {
        return today >= start && today <= end;
      }
    } else {
      const date = this.parseDate(deadlineStr.split(" ")[0].trim());
      if (date) {
        return date >= today && date <= todayEnd;
      }
    }
    return false;
  }
  parseDate(dateStr) {
    try {
      return new Date(dateStr);
    } catch (e) {
      return null;
    }
  }
  async createDailyNote() {
    const today = new Date();
    const dateStr = today.toISOString().split("T")[0];
    const userName = this.settings.currentUser || "default";
    const folderPath = `daily-${userName}`;
    const filePath = `${folderPath}/${dateStr}.md`;
    const existingFile = this.app.vault.getAbstractFileByPath(filePath);
    if (existingFile instanceof import_obsidian2.TFile) {
      return existingFile;
    }
    await this.ensureFolder(folderPath);
    const content = await this.buildDailyContent(today);
    return this.app.vault.create(filePath, content);
  }
  async ensureFolder(path2) {
    const folder = this.app.vault.getAbstractFileByPath(path2);
    if (!folder) {
      await this.app.vault.createFolder(path2);
    }
  }
  async buildDailyContent(today) {
    var _a;
    const routines = await this.parseRoutines();
    const todayTasks = await this.getTodayTasks();
    const dayKey = this.getDayKey(today);
    const dayName = this.getDayName(today);
    const weekNumber = this.getWeekNumber(today);
    const everyDayRoutine = routines.every_day || [];
    const dayRoutine = routines[dayKey] || [];
    const allDayRoutine = [...everyDayRoutine, ...dayRoutine];
    let tasksList = "";
    if (todayTasks.length === 0) {
      tasksList = "*\u041D\u0435\u0442 \u0437\u0430\u0434\u0430\u0447 \u043D\u0430 \u0441\u0435\u0433\u043E\u0434\u043D\u044F*";
    } else {
      for (const task of todayTasks) {
        const cache = this.app.metadataCache.getFileCache(task);
        const status = ((_a = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _a["status"]) || "To Do";
        const icon = this.getStatusIcon(status);
        tasksList += `- ${icon} [[${task.basename}]]
`;
      }
    }
    let scheduleList = "";
    if (allDayRoutine.length === 0) {
      scheduleList = "*\u0420\u0430\u0441\u043F\u0438\u0441\u0430\u043D\u0438\u0435 \u043D\u0435 \u0437\u0430\u0434\u0430\u043D\u043E*";
    } else {
      const sorted = [...allDayRoutine].sort((a, b) => {
        if (!a.time)
          return 1;
        if (!b.time)
          return -1;
        return a.time.localeCompare(b.time);
      });
      for (const task of sorted) {
        scheduleList += `- [ ] ${task.name}${task.time ? ` [${task.time}]` : ""}
`;
      }
    }
    const dateFormatted = today.toLocaleDateString("ru-RU", {
      day: "numeric",
      month: "long",
      year: "numeric"
    });
    return `---
daily: true
cssclasses: [hide-properties]
---

\`\`\`monitoring-duration
\`\`\`

# \u{1F4C5} ${dateFormatted} (${dayName}) \u2022 \u041D\u0435\u0434\u0435\u043B\u044F ${weekNumber}

---

## \u{1F550} \u0420\u0430\u0441\u043F\u0438\u0441\u0430\u043D\u0438\u0435 \u0434\u043D\u044F
${scheduleList}

---

## \u2705 \u0417\u0430\u0434\u0430\u0447\u0438 \u043D\u0430 \u0441\u0435\u0433\u043E\u0434\u043D\u044F
${tasksList}

---

## \u{1F4DD} \u0417\u0430\u043C\u0435\u0442\u043A\u0438

`;
  }
  getWeekNumber(date) {
    const startOfYear = new Date(date.getFullYear(), 0, 1);
    const pastDaysOfYear = (date.getTime() - startOfYear.getTime()) / 864e5;
    return Math.ceil((pastDaysOfYear + startOfYear.getDay() + 1) / 7);
  }
  getStatusIcon(status) {
    const s = status.toLowerCase();
    if (s.includes("\u0437\u0430\u0432\u0435\u0440\u0448\u0435\u043D") || s.includes("\u0432\u044B\u043F\u043E\u043B\u043D\u0435\u043D\u043E") || s === "done" || s === "completed")
      return "\u2705";
    if (s.includes("\u0432 \u0440\u0430\u0431\u043E\u0442\u0435") || s.includes("\u043F\u0440\u043E\u0446\u0435\u0441\u0441\u0435") || s === "active" || s === "in progress")
      return "\u{1F504}";
    return "\u2B1C";
  }
};

// src/settings/SettingsTab.ts
var import_obsidian3 = require("obsidian");
var DEFAULT_SETTINGS = {
  llmApiKey: "",
  llmBaseUrl: "https://api.ai.beeline.ru/api/v3",
  llmModel: "llm-medium-moe-instruct",
  mailFolder: "Inbox",
  pollInterval: 15,
  scannedEmailIds: [],
  dashboardNoteName: "Dashboard.md",
  useMockData: false,
  useMockLLM: false,
  chatSystemPrompt: "\u0422\u044B \u043F\u043E\u043B\u0435\u0437\u043D\u044B\u0439 \u043A\u043E\u0440\u043F\u043E\u0440\u0430\u0442\u0438\u0432\u043D\u044B\u0439 \u0418\u0418-\u0430\u0441\u0441\u0438\u0441\u0442\u0435\u043D\u0442. \u041E\u0442\u0432\u0435\u0447\u0430\u0439 \u0432\u0441\u0435\u0433\u0434\u0430 \u043D\u0430 \u0440\u0443\u0441\u0441\u043A\u043E\u043C \u044F\u0437\u044B\u043A\u0435, \u043F\u043E\u043C\u043E\u0433\u0430\u0439 \u0441 \u0430\u043D\u0430\u043B\u0438\u0437\u043E\u043C \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u043E\u0432 \u0438 \u043D\u0430\u043F\u0438\u0441\u0430\u043D\u0438\u0435\u043C \u0434\u043E\u043A\u0443\u043C\u0435\u043D\u0442\u0430\u0446\u0438\u0438.",
  chatTemperature: 0.7,
  incidentsFolder: "mail",
  simpleNotesFolder: "notes",
  savedFilters: [],
  currentUser: ""
};
var MonitoringSettingTab = class extends import_obsidian3.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    containerEl.createEl("h2", { text: "Monitoring Plugin Settings" });
    new import_obsidian3.Setting(containerEl).setName("LLM Base URL").setDesc("Base URL for the Corporate LLM API (OpenAI Compatible)").addText((text) => text.setPlaceholder("https://api.ai.beeline.ru/api/v3").setValue(this.plugin.settings.llmBaseUrl).onChange(async (value) => {
      this.plugin.settings.llmBaseUrl = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian3.Setting(containerEl).setName("LLM API Key").setDesc("Bearer token for the corporate LLM API").addText((text) => {
      text.setPlaceholder("Enter your API Key").setValue(this.plugin.settings.llmApiKey).onChange(async (value) => {
        this.plugin.settings.llmApiKey = value;
        await this.plugin.saveSettings();
      });
      text.inputEl.type = "password";
      return text;
    });
    new import_obsidian3.Setting(containerEl).setName("LLM Model").setDesc("Model name to use for summarization").addText((text) => text.setPlaceholder("llm-medium-moe-instruct").setValue(this.plugin.settings.llmModel).onChange(async (value) => {
      this.plugin.settings.llmModel = value;
      await this.plugin.saveSettings();
    }));
    containerEl.createEl("h3", { text: "AI Chat Settings" });
    new import_obsidian3.Setting(containerEl).setName("System Prompt").setDesc("System prompt given to the AI for chat conversations.").addTextArea((text) => text.setPlaceholder("Enter system prompt...").setValue(this.plugin.settings.chatSystemPrompt).onChange(async (value) => {
      this.plugin.settings.chatSystemPrompt = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian3.Setting(containerEl).setName("Temperature").setDesc("Variability of AI responses (0.0 to 1.0)").addSlider((slider) => slider.setLimits(0, 1, 0.1).setValue(this.plugin.settings.chatTemperature).setDynamicTooltip().onChange(async (value) => {
      this.plugin.settings.chatTemperature = value;
      await this.plugin.saveSettings();
    }));
    containerEl.createEl("h3", { text: "Outlook Integration" });
    new import_obsidian3.Setting(containerEl).setName("Outlook Folder Name").setDesc("The folder to check for new incident emails (e.g. Inbox, Alerts)").addText((text) => text.setPlaceholder("Inbox").setValue(this.plugin.settings.mailFolder).onChange(async (value) => {
      this.plugin.settings.mailFolder = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian3.Setting(containerEl).setName("Dashboard Note Name").setDesc("Name of the note containing tracked subjects (e.g. Dashboard.md)").addText((text) => text.setPlaceholder("Dashboard.md").setValue(this.plugin.settings.dashboardNoteName).onChange(async (value) => {
      this.plugin.settings.dashboardNoteName = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian3.Setting(containerEl).setName("Clear Scanned Emails Cache").setDesc("Reset the list of already processed emails so they can be scanned again.").addButton((button) => button.setButtonText("Clear Cache").onClick(async () => {
      this.plugin.settings.scannedEmailIds = [];
      await this.plugin.saveSettings();
      new import_obsidian3.Notice("Scanned emails cache cleared!");
    }));
    containerEl.createEl("h3", { text: "Notes settings" });
    new import_obsidian3.Setting(containerEl).setName("Incidents Folder").setDesc("Folder where new incident notes will be created. Leave empty to use vault root.").addText((text) => text.setPlaceholder("mail").setValue(this.plugin.settings.incidentsFolder).onChange(async (value) => {
      this.plugin.settings.incidentsFolder = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian3.Setting(containerEl).setName("Simple Notes Folder").setDesc("Folder where simple notes will be created and displayed on the dashboard.").addText((text) => text.setPlaceholder("notes").setValue(this.plugin.settings.simpleNotesFolder).onChange(async (value) => {
      this.plugin.settings.simpleNotesFolder = value;
      await this.plugin.saveSettings();
    }));
    containerEl.createEl("h3", { text: "\u041A\u043E\u043C\u0430\u043D\u0434\u0430" });
    new import_obsidian3.Setting(containerEl).setName("\u0412\u0430\u0448\u0435 \u0438\u043C\u044F").setDesc('\u0412\u0430\u0448\u0435 \u0438\u043C\u044F \u0434\u043B\u044F \u0444\u0438\u043B\u044C\u0442\u0440\u0430\u0446\u0438\u0438 \u0437\u0430\u0434\u0430\u0447 (\u0438\u0441\u043F\u043E\u043B\u044C\u0437\u0443\u0435\u0442\u0441\u044F \u0432 "\u041C\u043E\u0438 \u0437\u0430\u0434\u0430\u0447\u0438")').addText((text) => text.setPlaceholder("\u0412\u0430\u0448\u0435 \u0438\u043C\u044F").setValue(this.plugin.settings.currentUser).onChange(async (value) => {
      this.plugin.settings.currentUser = value;
      await this.plugin.saveSettings();
    }));
    containerEl.createEl("h3", { text: "Development / Testing" });
    new import_obsidian3.Setting(containerEl).setName("Use Mock Email Data").setDesc("Inject fake Outlook emails for local testing without Outlook installed.").addToggle((toggle) => toggle.setValue(this.plugin.settings.useMockData).onChange(async (value) => {
      this.plugin.settings.useMockData = value;
      await this.plugin.saveSettings();
    }));
    new import_obsidian3.Setting(containerEl).setName("Use Mock LLM").setDesc("Return dummy text instead of calling the corporate LLM.").addToggle((toggle) => toggle.setValue(this.plugin.settings.useMockLLM).onChange(async (value) => {
      this.plugin.settings.useMockLLM = value;
      await this.plugin.saveSettings();
    }));
  }
};

// src/chat/ChatView.ts
var import_obsidian4 = require("obsidian");
var CHAT_VIEW_TYPE = "monitoring-chat-view";
var ChatView = class extends import_obsidian4.ItemView {
  constructor(leaf, llmService) {
    super(leaf);
    this.messages = [];
    this.llmService = llmService;
  }
  getViewType() {
    return CHAT_VIEW_TYPE;
  }
  getDisplayText() {
    return "\u0418\u0418-\u0427\u0430\u0442 \u041C\u043E\u043D\u0438\u0442\u043E\u0440\u0438\u043D\u0433\u0430";
  }
  getIcon() {
    return "bot";
  }
  async onOpen() {
    const container = this.containerEl.children[1];
    container.empty();
    container.addClass("monitoring-chat-view");
    const header = container.createDiv("chat-header");
    header.createEl("h3", { text: "\u041A\u043E\u0440\u043F\u043E\u0440\u0430\u0442\u0438\u0432\u043D\u044B\u0439 \u0418\u0418-\u0410\u0441\u0441\u0438\u0441\u0442\u0435\u043D\u0442" });
    const clearBtn = header.createEl("button", { text: "\u041E\u0447\u0438\u0441\u0442\u0438\u0442\u044C \u0438\u0441\u0442\u043E\u0440\u0438\u044E", cls: "chat-clear-btn" });
    clearBtn.onclick = () => {
      this.messages = [];
      this.renderMessages();
    };
    this.messageContainer = container.createDiv("chat-message-container");
    const inputArea = container.createDiv("chat-input-area");
    this.inputEl = inputArea.createEl("textarea", {
      placeholder: "\u0412\u0432\u0435\u0434\u0438\u0442\u0435 \u0432\u0430\u0448 \u0437\u0430\u043F\u0440\u043E\u0441...",
      cls: "chat-input"
    });
    this.inputEl.addEventListener("keydown", (e) => {
      if (e.key === "Enter" && !e.shiftKey) {
        e.preventDefault();
        this.sendMessage();
      }
    });
    const sendBtn = inputArea.createEl("button", { cls: "chat-send-btn" });
    (0, import_obsidian4.setIcon)(sendBtn, "send");
    sendBtn.onclick = () => this.sendMessage();
    this.renderMessages();
  }
  async sendMessage() {
    const text = this.inputEl.value.trim();
    if (!text)
      return;
    this.inputEl.value = "";
    this.messages.push({ role: "user", content: text });
    this.renderMessages();
    const typingEl = this.messageContainer.createDiv("chat-message assistant typing");
    typingEl.createDiv().setText("\u0418\u0418 \u043F\u0435\u0447\u0430\u0442\u0430\u0435\u0442...");
    this.scrollToBottom();
    try {
      const responseText = await this.llmService.sendChatMessage(this.messages);
      typingEl.remove();
      this.messages.push({ role: "assistant", content: responseText });
      this.renderMessages();
    } catch (error) {
      typingEl.remove();
      new import_obsidian4.Notice("\u041E\u0448\u0438\u0431\u043A\u0430 \u043E\u0442\u043F\u0440\u0430\u0432\u043A\u0438 \u0441\u043E\u043E\u0431\u0449\u0435\u043D\u0438\u044F: " + error.message);
      console.error(error);
    }
  }
  renderMessages() {
    this.messageContainer.empty();
    if (this.messages.length === 0) {
      const emptyEl = this.messageContainer.createDiv("chat-empty");
      emptyEl.setText("\u0417\u0434\u0435\u0441\u044C \u043F\u043E\u043A\u0430 \u043F\u0443\u0441\u0442\u043E. \u041D\u0430\u043F\u0438\u0448\u0438\u0442\u0435 \u0447\u0442\u043E-\u043D\u0438\u0431\u0443\u0434\u044C!");
      return;
    }
    this.messages.forEach((msg) => {
      const msgEl = this.messageContainer.createDiv(`chat-message ${msg.role}`);
      const copyBtn = msgEl.createDiv("chat-message-copy-btn");
      (0, import_obsidian4.setIcon)(copyBtn, "copy");
      copyBtn.onclick = () => {
        navigator.clipboard.writeText(msg.content);
        new import_obsidian4.Notice("\u0422\u0435\u043A\u0441\u0442 \u0441\u043A\u043E\u043F\u0438\u0440\u043E\u0432\u0430\u043D \u0432 \u0431\u0443\u0444\u0435\u0440 \u043E\u0431\u043C\u0435\u043D\u0430");
      };
      const contentEl = msgEl.createDiv("chat-message-content");
      if (msg.role === "assistant") {
        import_obsidian4.MarkdownRenderer.renderMarkdown(msg.content, contentEl, "", this);
      } else {
        contentEl.setText(msg.content);
      }
    });
    this.scrollToBottom();
  }
  scrollToBottom() {
    this.messageContainer.scrollTop = this.messageContainer.scrollHeight;
  }
  async onClose() {
  }
};

// src/main-page/MainPageView.ts
var import_obsidian11 = require("obsidian");

// src/main-page/DataService.ts
var import_obsidian5 = require("obsidian");
var DataService = class {
  constructor(app, incidentsFolder, simpleNotesFolder = "notes") {
    this.app = app;
    this.incidentsFolder = incidentsFolder.toLowerCase();
    this.simpleNotesFolder = simpleNotesFolder.toLowerCase();
  }
  async fetchVaultData() {
    var _a, _b;
    const incidents = [];
    const tasks = [];
    const projects = [];
    const notes = [];
    const files = this.app.vault.getMarkdownFiles();
    for (const file of files) {
      const cache = this.app.metadataCache.getFileCache(file);
      let rawTags = ((_a = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _a.tags) || [];
      const tags = this.normalizeTags(rawTags);
      const cleanTags = this.cleanTags(tags);
      const filePathLower = file.path.toLowerCase();
      const fileBasename = file.basename;
      const linkedProject = (_b = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _b["linked_project"];
      if (this.isIncident(filePathLower, cleanTags)) {
        incidents.push(this.parseIncident(file, cache, fileBasename));
      } else if (this.isTask(cleanTags, filePathLower, linkedProject)) {
        tasks.push(this.parseTask(file, cache, fileBasename, cleanTags, linkedProject));
      } else if (this.isProject(cleanTags, filePathLower)) {
        projects.push(this.parseProject(file, cache, fileBasename));
      } else if (this.isNote(cleanTags, filePathLower)) {
        notes.push(this.parseNote(file, cache, fileBasename, cleanTags));
      }
    }
    return {
      incidents: incidents.sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime()),
      tasks: tasks.sort((a, b) => a.name.localeCompare(b.name)),
      projects: projects.sort((a, b) => a.name.localeCompare(b.name)),
      notes: notes.sort((a, b) => a.name.localeCompare(b.name))
    };
  }
  normalizeTags(rawTags) {
    if (typeof rawTags === "string") {
      return rawTags.split(/[,;]/).map((t) => t.trim());
    } else if (Array.isArray(rawTags)) {
      return rawTags.map((t) => String(t).trim());
    }
    return [];
  }
  cleanTags(tags) {
    return tags.filter((t) => t && t.length > 0).map((t) => t.startsWith("#") ? t.substring(1).toLowerCase() : t.toLowerCase());
  }
  isIncident(path2, tags) {
    return path2.startsWith(this.incidentsFolder) || tags.includes("incident");
  }
  isTask(tags, path2, linkedProject) {
    return tags.includes("task") || path2.startsWith("tasks/") || !!linkedProject;
  }
  isProject(tags, path2) {
    return tags.includes("project") || path2.startsWith("projects/");
  }
  isNote(tags, path2) {
    return tags.includes("note") || this.simpleNotesFolder.length > 0 && path2.startsWith(this.simpleNotesFolder);
  }
  parseIncident(file, cache, basename) {
    var _a, _b, _c, _d;
    return {
      id: basename.replace(/^Incident-/, ""),
      name: ((_a = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _a["conversation_topic"]) || basename,
      status: ((_b = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _b["status"]) || "Unknown",
      date: ((_c = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _c["date"]) || "Unknown",
      path: file.path,
      sender: ((_d = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _d["sender"]) || "Unknown"
    };
  }
  parseTask(file, cache, basename, tags, linkedProject) {
    var _a, _b, _c, _d;
    return {
      id: basename,
      name: basename.replace(/^Task-/, ""),
      status: ((_a = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _a["status"]) || "To Do",
      deadline: ((_b = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _b["deadline"]) || "",
      path: file.path,
      priority: ((_c = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _c["priority"]) || 3,
      tags,
      linkedProject,
      responsible: ((_d = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _d["responsible"]) || void 0
    };
  }
  parseProject(file, cache, basename) {
    var _a, _b, _c, _d, _e, _f;
    return {
      id: basename,
      name: basename.replace(/^Project-/, ""),
      status: ((_a = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _a["status"]) || "Active",
      path: file.path,
      goal: ((_b = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _b["goal"]) || "",
      priority: (_c = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _c["priority"],
      target_date: (_d = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _d["target_date"],
      responsible: ((_e = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _e["responsible"]) || void 0,
      tracked_emails: ((_f = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _f["tracked_emails"]) || []
    };
  }
  parseNote(file, cache, basename, tags) {
    var _a, _b;
    return {
      id: basename,
      name: basename,
      path: file.path,
      created: ((_a = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _a["created"]) || "",
      tags,
      author: ((_b = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _b["author"]) || void 0
    };
  }
  async updateItemStatus(path2, newStatus) {
    const file = this.app.vault.getAbstractFileByPath(path2);
    if (file instanceof import_obsidian5.TFile) {
      await this.app.fileManager.processFrontMatter(file, (fm) => {
        fm["status"] = newStatus;
      });
    }
  }
  async getTrackedSubjects(dashboardFn) {
    const dashboardFile = this.app.vault.getAbstractFileByPath(dashboardFn);
    if (!dashboardFile)
      return [];
    const cache = this.app.metadataCache.getFileCache(dashboardFile);
    if ((cache == null ? void 0 : cache.frontmatter) && Array.isArray(cache.frontmatter["tracked_subjects"])) {
      return cache.frontmatter["tracked_subjects"];
    }
    return [];
  }
};

// src/main-page/DashboardView.ts
var import_obsidian7 = require("obsidian");

// src/main-page/BaseView.ts
var import_obsidian6 = require("obsidian");
var BaseView = class {
  constructor(app) {
    this.app = app;
  }
  getStatusClass(status) {
    status = status.toLowerCase();
    if (status.includes("\u0437\u0430\u0432\u0435\u0440\u0448\u0435\u043D") || status.includes("\u0432\u044B\u043F\u043E\u043B\u043D\u0435\u043D\u043E") || status === "done" || status === "completed")
      return "status-success";
    if (status.includes("\u0432 \u0440\u0430\u0431\u043E\u0442\u0435") || status.includes("\u0432 \u043F\u0440\u043E\u0446\u0435\u0441\u0441\u0435") || status === "active" || status === "in progress")
      return "status-active";
    if (status.includes("\u0437\u0430\u043F\u043B\u0430\u043D\u0438\u0440\u043E\u0432\u0430\u043D\u043E") || status.includes("\u043E\u0436\u0438\u0434\u0430\u0435\u0442") || status === "pending")
      return "status-pending";
    return "status-default";
  }
  openFile(path2) {
    const file = this.app.vault.getAbstractFileByPath(path2);
    if (file instanceof import_obsidian6.TFile) {
      this.app.workspace.getLeaf(false).openFile(file);
    }
  }
  createEmptyState(container, message) {
    container.createEl("p", { text: message, cls: "empty-state-text" });
  }
  createHeader(container, title) {
    return container.createEl("h3", { text: title });
  }
  createTable(container, headers) {
    const table = container.createEl("table", { cls: "monitoring-data-table" });
    const thead = table.createEl("thead");
    const headerRow = thead.createEl("tr");
    headers.forEach((h) => headerRow.createEl("th", { text: h }));
    const tbody = table.createEl("tbody");
    return { thead, tbody };
  }
  createLinkCell(cell, text, path2) {
    const link = cell.createEl("a", { text, cls: "incident-link" });
    link.onclick = (e) => {
      e.preventDefault();
      this.openFile(path2);
    };
    return link;
  }
  createStatusBadge(cell, status) {
    return cell.createSpan({ text: status, cls: `status-badge ${this.getStatusClass(status)}` });
  }
};

// src/main-page/DashboardView.ts
var DashboardView = class extends BaseView {
  constructor(app, dataService, incidents, tasks, projects, savedFilters, currentFilterId, dashboardFn) {
    super(app);
    this.dataService = dataService;
    this.incidents = incidents;
    this.tasks = tasks;
    this.projects = projects;
    this.savedFilters = savedFilters;
    this.currentFilterId = currentFilterId;
    this.dashboardFn = dashboardFn;
  }
  render(container) {
    this.renderHeroSection(container);
    this.renderProjectsSection(container);
    this.renderTasksSection(container);
    this.renderMailSection(container);
  }
  renderHeroSection(container) {
    const hero = container.createDiv({ cls: "monitoring-hero-section" });
    const leftSide = hero.createDiv({ cls: "hero-left" });
    const now = new Date();
    leftSide.createEl("h1", {
      text: now.toLocaleDateString("ru-RU", { weekday: "long", day: "numeric", month: "long" }),
      cls: "hero-title"
    });
    leftSide.createEl("p", {
      text: `\u0410\u043A\u0442\u0443\u0430\u043B\u044C\u043D\u044B\u0435 \u0434\u0430\u043D\u043D\u044B\u0435 \u043D\u0430 ${now.toLocaleTimeString("ru-RU", { hour: "2-digit", minute: "2-digit" })}`,
      cls: "hero-subtitle"
    });
    const metrics = hero.createDiv({ cls: "hero-metrics" });
    const activeTasks = this.tasks.filter(
      (t) => t.status.toLowerCase().includes("progress") || t.status.toLowerCase().includes("\u0440\u0430\u0431\u043E\u0442\u0430")
    ).length;
    const pendingIncidents = this.incidents.filter(
      (i) => i.status.toLowerCase().includes("pending") || i.status.toLowerCase().includes("\u043E\u0436\u0438\u0434\u0430\u0435\u0442")
    ).length;
    const completedThisWeek = this.tasks.filter(
      (t) => t.status.toLowerCase().includes("done") || t.status.toLowerCase().includes("\u0437\u0430\u0432\u0435\u0440\u0448\u0435\u043D")
    ).length;
    this.createMetricItem(metrics, activeTasks.toString(), "\u0417\u0430\u0434\u0430\u0447 \u0432 \u0440\u0430\u0431\u043E\u0442\u0435", "blue-glow");
    this.createMetricItem(metrics, pendingIncidents.toString(), "\u0418\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u043E\u0432", "orange-glow");
    this.createMetricItem(metrics, completedThisWeek.toString(), "\u0413\u043E\u0442\u043E\u0432\u043E (\u043D\u0435\u0434)", "green-glow");
  }
  createMetricItem(container, value, label, glowClass) {
    const item = container.createDiv({ cls: `metric-item ${glowClass}` });
    item.createDiv({ cls: "metric-value", text: value });
    item.createDiv({ cls: "metric-label", text: label });
  }
  renderProjectsSection(container) {
    container.createEl("h3", { text: "\u041F\u0440\u043E\u0435\u043A\u0442\u044B" });
    if (this.projects.length === 0) {
      this.createEmptyState(container, "\u041F\u0440\u043E\u0435\u043A\u0442\u043E\u0432 \u043F\u043E\u043A\u0430 \u043D\u0435\u0442.");
      return;
    }
    const { tbody } = this.createTable(container, ["\u041D\u0430\u0437\u0432\u0430\u043D\u0438\u0435", "\u0426\u0435\u043B\u044C", "\u041F\u043E\u0447\u0442\u0430", "\u0421\u0442\u0430\u0442\u0443\u0441"]);
    this.projects.forEach((p) => {
      const row = tbody.createEl("tr");
      const nameCell = row.createEl("td");
      this.createLinkCell(nameCell, p.name, p.path);
      row.createEl("td", { text: p.goal || "---" });
      const emailCell = row.createEl("td");
      const emails = p.tracked_emails || [];
      if (emails.length > 0) {
        emailCell.createSpan({ text: `\u{1F4E7} ${emails.length}`, cls: "email-count-badge" });
      } else {
        emailCell.createSpan({ text: "---", cls: "empty-text" });
      }
      this.createStatusBadge(row.createEl("td"), p.status);
    });
  }
  renderTasksSection(container) {
    const taskHeaderContainer = container.createDiv({ cls: "section-header-with-btn" });
    taskHeaderContainer.createEl("h3", { text: "\u0417\u0430\u0434\u0430\u0447\u0438" });
    const filterBtn = taskHeaderContainer.createEl("button", {
      cls: "monitoring-glass-btn filter-edit-btn",
      text: "\u2699\uFE0F \u0420\u0435\u0434\u0430\u043A\u0442\u0438\u0440\u043E\u0432\u0430\u0442\u044C \u0444\u0438\u043B\u044C\u0442\u0440\u044B"
    });
    filterBtn.onclick = () => this.openFilterModal();
    this.renderFilterTabs(container);
    this.renderTasksTable(container);
  }
  renderFilterTabs(container) {
    if (this.savedFilters.length === 0)
      return;
    const tabsContainer = container.createDiv({ cls: "filter-tabs-nav" });
    const allTab = tabsContainer.createDiv({
      cls: `filter-tab-btn ${this.currentFilterId === null ? "is-active" : ""}`,
      text: "\u0412\u0441\u0435"
    });
    allTab.onclick = () => {
      this.currentFilterId = null;
    };
    this.savedFilters.forEach((f, idx) => {
      const tab = tabsContainer.createDiv({
        cls: `filter-tab-btn ${this.currentFilterId === idx ? "is-active" : ""}`,
        text: f.name
      });
      tab.onclick = () => {
        this.currentFilterId = this.currentFilterId === idx ? null : idx;
      };
    });
  }
  renderTasksTable(container) {
    let filteredTasks = this.tasks;
    if (this.currentFilterId !== null && this.savedFilters[this.currentFilterId]) {
      const activeFilter = this.savedFilters[this.currentFilterId];
      const filterTags = activeFilter.tags.map((t) => t.trim().replace(/^#/, "").toLowerCase());
      const projectTag = filterTags.find((t) => t.startsWith("project"));
      const filterProjName = projectTag ? projectTag.replace(/^project/, "") : null;
      filteredTasks = this.tasks.filter((t) => {
        const tagsMatch = filterTags.every((fTag) => t.tags.includes(fTag));
        let projectMatch = false;
        if (filterProjName && t.linkedProject) {
          const cleanLinkProj = t.linkedProject.toLowerCase().replace(/\s+/g, "").replace(/[^\w\u0400-\u04FF]/g, "");
          if (cleanLinkProj === filterProjName)
            projectMatch = true;
        }
        return tagsMatch || projectMatch;
      });
    }
    if (filteredTasks.length === 0) {
      this.createEmptyState(container, "\u041D\u0435\u0442 \u0437\u0430\u0434\u0430\u0447, \u0441\u043E\u043E\u0442\u0432\u0435\u0442\u0441\u0442\u0432\u0443\u044E\u0449\u0438\u0445 \u0444\u0438\u043B\u044C\u0442\u0440\u0430\u043C.");
      return;
    }
    const { tbody } = this.createTable(container, ["\u0417\u0430\u0434\u0430\u0447\u0430", "\u0421\u0440\u043E\u043A", "\u041F\u0440\u0438\u043E\u0440\u0438\u0442\u0435\u0442", "\u0421\u0442\u0430\u0442\u0443\u0441"]);
    filteredTasks.slice(0, 50).forEach((t) => {
      const row = tbody.createEl("tr");
      const nameCell = row.createEl("td");
      this.createLinkCell(nameCell, t.name, t.path);
      row.createEl("td", { text: t.deadline || "---" });
      const pCell = row.createEl("td");
      pCell.createSpan({ text: t.priority.toString(), cls: `priority-badge priority-${t.priority}` });
      this.createStatusBadge(row.createEl("td"), t.status);
    });
  }
  renderMailSection(container) {
    container.createEl("h3", { text: "\u041F\u043E\u0447\u0442\u0430" });
    this.renderTrackedSubjects(container);
    this.renderIncidentsTable(container);
  }
  async renderTrackedSubjects(container) {
    const tracked = await this.dataService.getTrackedSubjects(this.dashboardFn);
    if (tracked.length === 0) {
      this.createEmptyState(container, "\u0422\u0435\u043C\u044B \u043D\u0435 \u043E\u0442\u0441\u043B\u0435\u0436\u0438\u0432\u0430\u044E\u0442\u0441\u044F. \u0414\u043E\u0431\u0430\u0432\u044C\u0442\u0435 \u0438\u0445 \u0432 Dashboard.md");
      return;
    }
    const list = container.createDiv({ cls: "tracked-subjects-list" });
    tracked.forEach((t) => {
      list.createSpan({ text: t, cls: "tracked-subject-tag" });
    });
  }
  renderIncidentsTable(container) {
    if (this.incidents.length === 0) {
      this.createEmptyState(container, "\u0418\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u043E\u0432 \u043F\u043E\u043A\u0430 \u043D\u0435\u0442.");
      return;
    }
    const { tbody } = this.createTable(container, ["\u0414\u0430\u0442\u0430", "\u0422\u0435\u043C\u0430 / \u0418\u043D\u0446\u0438\u0434\u0435\u043D\u0442", "\u041E\u0442\u043F\u0440\u0430\u0432\u0438\u0442\u0435\u043B\u044C", "\u0421\u0442\u0430\u0442\u0443\u0441"]);
    this.incidents.slice(0, 15).forEach((i) => {
      const row = tbody.createEl("tr");
      const dateStr = i.date !== "Unknown" ? new Date(i.date).toLocaleDateString("ru-RU") : "Unknown";
      row.createEl("td", { text: dateStr });
      const nameCell = row.createEl("td");
      this.createLinkCell(nameCell, i.name, i.path);
      row.createEl("td", { text: i.sender });
      this.createStatusBadge(row.createEl("td"), i.status);
    });
  }
  openFilterModal() {
    new FilterModal(this.app, this.projects, this.savedFilters, () => {
    }).open();
  }
};
var FilterModal = class extends import_obsidian7.Modal {
  constructor(app, projects, savedFilters, onSave) {
    super(app);
    this.currentName = "";
    this.currentTags = [];
    this.newTag = "";
    this.projects = projects;
    this.savedFilters = savedFilters;
    this.editingFilters = JSON.parse(JSON.stringify(savedFilters));
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.createEl("h3", { text: "\u0423\u043F\u0440\u0430\u0432\u043B\u0435\u043D\u0438\u0435 \u0441\u043E\u0445\u0440\u0430\u043D\u0435\u043D\u043D\u044B\u043C\u0438 \u0444\u0438\u043B\u044C\u0442\u0440\u0430\u043C\u0438" });
    const listContainer = contentEl.createDiv({ cls: "modal-filter-manager" });
    this.renderList(listContainer);
    contentEl.createEl("hr");
    contentEl.createEl("h4", { text: "\u0421\u043E\u0437\u0434\u0430\u0442\u044C \u043D\u043E\u0432\u044B\u0439 \u0444\u0438\u043B\u044C\u0442\u0440" });
    const form = contentEl.createDiv({ cls: "filter-creation-form" });
    const nameInput = new import_obsidian7.TextComponent(form);
    nameInput.setPlaceholder("\u041D\u0430\u0437\u0432\u0430\u043D\u0438\u0435 \u0444\u0438\u043B\u044C\u0442\u0440\u0430");
    nameInput.inputEl.style.width = "100%";
    nameInput.inputEl.style.marginBottom = "10px";
    nameInput.onChange((v) => this.currentName = v);
    const tagsPreview = form.createDiv({ cls: "active-filters-container", attr: { style: "min-height: 20px; margin-bottom: 10px;" } });
    this.renderTagsPreview(tagsPreview);
    const tagControls = form.createDiv({ attr: { style: "display: flex; gap: 8px; margin-bottom: 10px;" } });
    const tagInput = new import_obsidian7.TextComponent(tagControls);
    tagInput.setPlaceholder("\u0414\u043E\u0431\u0430\u0432\u0438\u0442\u044C \u0442\u0435\u0433...");
    tagInput.onChange((v) => this.newTag = v);
    new import_obsidian7.ButtonComponent(tagControls).setButtonText("\u0414\u043E\u0431\u0430\u0432\u0438\u0442\u044C").onClick(() => {
      if (this.newTag) {
        const t = this.newTag.trim().replace(/^#/, "");
        if (!this.currentTags.includes(t)) {
          this.currentTags.push(t);
          this.renderTagsPreview(tagsPreview);
        }
        this.newTag = "";
        tagInput.setValue("");
      }
    });
    new import_obsidian7.ButtonComponent(form).setButtonText("\u0414\u043E\u0431\u0430\u0432\u0438\u0442\u044C \u0444\u0438\u043B\u044C\u0442\u0440 \u0432 \u0441\u043F\u0438\u0441\u043E\u043A").onClick(() => {
      if (!this.currentName || this.currentTags.length === 0) {
        new import_obsidian7.Notice("\u0423\u043A\u0430\u0436\u0438\u0442\u0435 \u043D\u0430\u0437\u0432\u0430\u043D\u0438\u0435 \u0438 \u0445\u043E\u0442\u044F \u0431\u044B \u043E\u0434\u0438\u043D \u0442\u0435\u0433");
        return;
      }
      this.editingFilters.unshift({ name: this.currentName, tags: [...this.currentTags] });
      this.currentName = "";
      this.currentTags = [];
      nameInput.setValue("");
      this.renderTagsPreview(tagsPreview);
      this.renderList(listContainer);
    }).buttonEl.style.width = "100%";
    const footer = contentEl.createDiv({ cls: "modal-button-container", attr: { style: "display: flex; justify-content: flex-end; gap: 10px; margin-top: 30px;" } });
    new import_obsidian7.ButtonComponent(footer).setButtonText("\u0421\u043E\u0445\u0440\u0430\u043D\u0438\u0442\u044C").setCta().onClick(() => this.close());
    new import_obsidian7.ButtonComponent(footer).setButtonText("\u041E\u0442\u043C\u0435\u043D\u0430").onClick(() => this.close());
  }
  renderList(container) {
    container.empty();
    if (this.editingFilters.length === 0) {
      container.createEl("p", { text: "\u0423 \u0432\u0430\u0441 \u043F\u043E\u043A\u0430 \u043D\u0435\u0442 \u0441\u043E\u0445\u0440\u0430\u043D\u0435\u043D\u043D\u044B\u0445 \u0444\u0438\u043B\u044C\u0442\u0440\u043E\u0432", cls: "empty-state-text" });
    }
    this.editingFilters.forEach((f, idx) => {
      const item = container.createDiv({ cls: "modal-filter-mgmt-item" });
      const mainInfo = item.createDiv({ cls: "mgmt-item-info" });
      mainInfo.createEl("b", { text: f.name });
      mainInfo.createEl("br");
      mainInfo.createSpan({ text: f.tags.map((t) => `#${t}`).join(" "), cls: "mgmt-item-tags" });
      const deleteBtn = item.createEl("button", { text: "\u0423\u0434\u0430\u043B\u0438\u0442\u044C", cls: "footer-btn delete-btn" });
      deleteBtn.onclick = () => {
        this.editingFilters.splice(idx, 1);
        this.renderList(container);
      };
    });
  }
  renderTagsPreview(container) {
    container.empty();
    this.currentTags.forEach((t) => {
      const tagEl = container.createSpan({ cls: "filter-tag", text: `#${t}` });
      const x = tagEl.createSpan({ text: " \xD7", cls: "filter-tag-remove" });
      x.onclick = () => {
        this.currentTags = this.currentTags.filter((tag) => tag !== t);
        this.renderTagsPreview(container);
      };
    });
  }
};

// src/main-page/KanbanView.ts
var import_obsidian9 = require("obsidian");

// src/team/TeamService.ts
var import_obsidian8 = require("obsidian");
var TeamService = class {
  constructor(app) {
    this.app = app;
  }
  async getTeamMembers() {
    const routinesFile = this.app.vault.getAbstractFileByPath("routines.md");
    if (!routinesFile || !(routinesFile instanceof import_obsidian8.TFile)) {
      return [];
    }
    try {
      const content = await this.app.vault.read(routinesFile);
      return this.parseTeamFromContent(content);
    } catch (e) {
      console.error("Error reading routines.md:", e);
      return [];
    }
  }
  getCurrentUser() {
    const settings = this.getSettings();
    return (settings == null ? void 0 : settings.currentUser) || "";
  }
  async addTeamMember(name) {
    const routinesFile = this.app.vault.getAbstractFileByPath("routines.md");
    if (!routinesFile || !(routinesFile instanceof import_obsidian8.TFile)) {
      const content2 = `# \u041A\u043E\u043C\u0430\u043D\u0434\u0430
- ${name}
`;
      await this.app.vault.create("routines.md", content2);
      return;
    }
    const content = await this.app.vault.read(routinesFile);
    if (!content.includes("# \u041A\u043E\u043C\u0430\u043D\u0434\u0430") && !content.includes("# \u043A\u043E\u043C\u0430\u043D\u0434\u0430")) {
      const newContent = content + "\n# \u041A\u043E\u043C\u0430\u043D\u0434\u0430\n- " + name + "\n";
      await this.app.vault.modify(routinesFile, newContent);
    } else if (!content.includes("- " + name)) {
      const lines = content.split("\n");
      let inTeamSection = false;
      const newLines = [];
      for (const line of lines) {
        if (line.trim().toLowerCase() === "# \u043A\u043E\u043C\u0430\u043D\u0434\u0430") {
          inTeamSection = true;
        }
        if (inTeamSection && (line.startsWith("# ") || line.startsWith("## "))) {
          newLines.push(line);
          newLines.push("- " + name);
          inTeamSection = false;
          continue;
        }
        newLines.push(line);
      }
      if (inTeamSection) {
        newLines.push("- " + name);
      }
      await this.app.vault.modify(routinesFile, newLines.join("\n"));
    }
  }
  parseTeamFromContent(content) {
    const members = [];
    let inTeamSection = false;
    const lines = content.split("\n");
    for (const line of lines) {
      const trimmed = line.trim();
      if (trimmed.toLowerCase() === "# \u043A\u043E\u043C\u0430\u043D\u0434\u0430" || trimmed.toLowerCase() === "# team") {
        inTeamSection = true;
        continue;
      }
      if (inTeamSection) {
        if (trimmed.startsWith("# ") || trimmed.startsWith("## ")) {
          inTeamSection = false;
          continue;
        }
        const match = trimmed.match(/^[-*]\s*(.+)$/);
        if (match && match[1].trim()) {
          members.push(match[1].trim());
        }
      }
    }
    return members;
  }
  getSettings() {
    var _a;
    return ((_a = this.app.plugins.plugins["monitoring-plugin"]) == null ? void 0 : _a.settings) || null;
  }
};

// src/main-page/KanbanView.ts
var KanbanView = class extends BaseView {
  constructor(app, dataService, templateManager, tasks, projects, onRefresh, teamMembers = [], currentUser = "") {
    super(app);
    this.teamMembers = [];
    this.currentUser = "";
    this.statuses = [
      { id: "todo", label: "\u041E\u0436\u0438\u0434\u0430\u043D\u0438\u0435", color: "orange", match: ["todo", "pending", "\u043E\u0436\u0438\u0434\u0430", "\u0437\u0430\u043F\u043B\u0430\u043D\u0438\u0440\u043E\u0432\u0430\u043D\u043E", "to do"] },
      { id: "active", label: "\u0412 \u0440\u0430\u0431\u043E\u0442\u0435", color: "blue", match: ["active", "in progress", "\u0432 \u0440\u0430\u0431\u043E\u0442\u0435", "\u0432 \u043F\u0440\u043E\u0446\u0435\u0441\u0441\u0435"] },
      { id: "done", label: "\u0417\u0430\u0432\u0435\u0440\u0448\u0435\u043D\u043E", color: "green", match: ["done", "completed", "\u0437\u0430\u0432\u0435\u0440\u0448\u0435\u043D", "\u0432\u044B\u043F\u043E\u043B\u043D\u0435\u043D\u043E"] }
    ];
    this.dataService = dataService;
    this.templateManager = templateManager;
    this.tasks = tasks;
    this.projects = projects;
    this.onRefresh = onRefresh;
    this.teamService = new TeamService(app);
    this.teamMembers = teamMembers;
    this.currentUser = currentUser;
  }
  render(container) {
    this.renderBoard(container, "\u0414\u043E\u0441\u043A\u0430 \u041F\u0440\u043E\u0435\u043A\u0442\u043E\u0432", this.projects, "project");
    this.renderBoard(container, "\u0414\u043E\u0441\u043A\u0430 \u0417\u0430\u0434\u0430\u0447", this.tasks, "task");
  }
  renderBoard(container, title, items, type) {
    const wrapper = document.createElement("div");
    wrapper.className = "kanban-section";
    const heading = document.createElement("h3");
    heading.textContent = title;
    heading.style.marginTop = "20px";
    wrapper.appendChild(heading);
    const board = wrapper.createDiv({ cls: "monitoring-kanban-board" });
    this.statuses.forEach((status) => {
      const column = board.createDiv({ cls: `kanban-column ${status.color}` });
      column.createEl("h4", { text: status.label });
      const cardsContainer = column.createDiv({ cls: "kanban-cards" });
      this.setupDragDrop(column, cardsContainer, status, items, type);
      const filtered = this.filterByStatus(items, status);
      if (filtered.length === 0) {
        cardsContainer.createDiv({ text: "\u041F\u0443\u0441\u0442\u043E", cls: "kanban-empty" });
      }
      filtered.forEach((item) => {
        this.renderCard(cardsContainer, item, type);
      });
    });
    container.appendChild(wrapper);
  }
  filterByStatus(items, status) {
    return items.filter((item) => {
      const s = (item.status || "").toLowerCase().trim();
      const labelLower = status.label.toLowerCase();
      return status.match.some((m) => s.includes(m)) || s === labelLower || s.replace(/\s+/g, "") === status.id;
    });
  }
  setupDragDrop(column, cardsContainer, status, items, type) {
    column.addEventListener("dragover", (e) => {
      e.preventDefault();
      column.addClass("is-drag-over");
    });
    column.addEventListener("dragleave", () => {
      column.removeClass("is-drag-over");
    });
    column.addEventListener("drop", async (e) => {
      var _a;
      e.preventDefault();
      column.removeClass("is-drag-over");
      try {
        const rawData = (_a = e.dataTransfer) == null ? void 0 : _a.getData("text/plain");
        if (!rawData)
          return;
        const data = JSON.parse(rawData);
        if (data.path && data.type === type) {
          const file = this.app.vault.getAbstractFileByPath(data.path);
          const statusValue = status.id === "todo" ? "To Do" : status.id === "active" ? "In Progress" : status.id === "done" ? "Done" : status.label;
          await this.dataService.updateItemStatus(data.path, statusValue);
          if (this.templateManager && file instanceof import_obsidian9.TFile && file.basename.startsWith("Task-")) {
            await this.templateManager.updateSubtaskStatusIcon(file);
          }
          await new Promise((r) => setTimeout(r, 400));
          this.onRefresh();
        }
      } catch (err) {
        console.error("Drop error:", err);
      }
    });
  }
  renderCard(cardsContainer, item, type) {
    const cardClass = type === "task" ? "task-card priority-" + item.priority : "project-card";
    const card = cardsContainer.createDiv({
      cls: "kanban-card " + cardClass
    });
    if (type === "project" && item.priority) {
      card.addClass(`priority-${item.priority}`);
    }
    card.setAttribute("draggable", "true");
    card.createDiv({ cls: "card-title", text: item.name });
    let metaText = "";
    if (type === "task") {
      metaText = item.deadline || "\u0411\u0435\u0437 \u0441\u0440\u043E\u043A\u0430";
    } else {
      metaText = item.goal || "\u0426\u0435\u043B\u044C \u043D\u0435 \u0437\u0430\u0434\u0430\u043D\u0430";
    }
    if (item.responsible) {
      metaText += ` | \u{1F464} ${item.responsible}`;
    }
    card.createDiv({ cls: "card-meta", text: metaText });
    card.addEventListener("dragstart", (e) => {
      var _a;
      (_a = e.dataTransfer) == null ? void 0 : _a.setData("text/plain", JSON.stringify({ path: item.path, type }));
      card.addClass("is-dragging");
    });
    card.addEventListener("dragend", () => {
      card.removeClass("is-dragging");
    });
    card.onclick = () => {
      this.openFile(item.path);
    };
    card.oncontextmenu = (e) => {
      e.preventDefault();
      this.showResponsibleModal(item, type);
    };
  }
  async showResponsibleModal(item, type) {
    new ResponsibleModal(
      this.app,
      item,
      type,
      async (newResponsible) => {
        await this.updateResponsible(item.path, newResponsible);
        this.onRefresh();
      },
      this.teamMembers,
      this.currentUser
    ).open();
  }
  async updateResponsible(path2, responsible) {
    const file = this.app.vault.getAbstractFileByPath(path2);
    if (!file || !(file instanceof import_obsidian9.TFile))
      return;
    const content = await this.app.vault.read(file);
    const lines = content.split("\n");
    const newLines = [];
    let inFrontmatter = false;
    let frontmatterEnded = false;
    for (const line of lines) {
      if (line.trim() === "---" && !inFrontmatter) {
        inFrontmatter = true;
        newLines.push(line);
        continue;
      }
      if (line.trim() === "---" && inFrontmatter && !frontmatterEnded) {
        frontmatterEnded = true;
        newLines.push(line);
        continue;
      }
      if (inFrontmatter && !frontmatterEnded) {
        if (line.match(/^responsible:/)) {
          if (responsible) {
            newLines.push(`responsible: "${responsible}"`);
          }
          continue;
        }
      }
      newLines.push(line);
    }
    if (inFrontmatter && !frontmatterEnded) {
      if (responsible) {
        newLines.push(`responsible: "${responsible}"`);
      }
      newLines.push("---");
    } else if (responsible) {
      const insertIdx = newLines.findIndex((l) => l.trim() === "---");
      if (insertIdx !== -1) {
        newLines.splice(insertIdx + 1, 0, `responsible: "${responsible}"`);
      }
    }
    await this.app.vault.modify(file, newLines.join("\n"));
  }
};
var ResponsibleModal = class extends import_obsidian9.Modal {
  constructor(app, item, type, onSave, teamMembers = [], currentUser = "") {
    super(app);
    this.teamMembers = [];
    this.currentUser = "";
    this.item = item;
    this.type = type;
    this.onSave = onSave;
    this.teamMembers = teamMembers;
    this.currentUser = currentUser;
  }
  async onOpen() {
    const { contentEl } = this;
    contentEl.createEl("h3", { text: `\u041E\u0442\u0432\u0435\u0442\u0441\u0442\u0432\u0435\u043D\u043D\u044B\u0439: ${this.item.name}` });
    const selectContainer = contentEl.createDiv();
    selectContainer.style.marginBottom = "20px";
    const select = selectContainer.createEl("select");
    select.style.width = "100%";
    select.style.padding = "8px";
    select.style.marginBottom = "16px";
    const emptyOption = select.createEl("option");
    emptyOption.text = "\u041D\u0435 \u043D\u0430\u0437\u043D\u0430\u0447\u0435\u043D";
    emptyOption.value = "";
    if (!this.item.responsible)
      emptyOption.selected = true;
    const allMembers = [...this.teamMembers];
    if (this.currentUser && !allMembers.includes(this.currentUser)) {
      allMembers.push(this.currentUser);
    }
    allMembers.forEach((member) => {
      const option = select.createEl("option");
      option.text = member;
      option.value = member;
      if (this.item.responsible === member) {
        option.selected = true;
      }
    });
    if (this.item.responsible && !this.teamMembers.includes(this.item.responsible)) {
      const customOption = select.createEl("option");
      customOption.text = this.item.responsible + " (\u0442\u0435\u043A\u0443\u0449\u0438\u0439)";
      customOption.value = this.item.responsible;
      customOption.selected = true;
    }
    const customContainer = contentEl.createDiv();
    customContainer.style.marginBottom = "20px";
    const span = customContainer.createEl("span");
    span.textContent = "\u0418\u043B\u0438 \u0432\u0432\u0435\u0434\u0438\u0442\u0435 \u043D\u043E\u0432\u043E\u0435 \u0438\u043C\u044F:";
    span.style.display = "block";
    span.style.marginBottom = "8px";
    const input = new import_obsidian9.TextComponent(customContainer);
    input.setPlaceholder("\u041D\u043E\u0432\u043E\u0435 \u0438\u043C\u044F \u043E\u0442\u0432\u0435\u0442\u0441\u0442\u0432\u0435\u043D\u043D\u043E\u0433\u043E");
    input.inputEl.style.width = "100%";
    const addTeamBtn = new import_obsidian9.ButtonComponent(contentEl).setButtonText("+ \u0414\u043E\u0431\u0430\u0432\u0438\u0442\u044C \u0432 \u043A\u043E\u043C\u0430\u043D\u0434\u0443").setClass("mod-small").onClick(async () => {
      const newName = input.inputEl.value.trim();
      if (newName) {
        await this.addToTeam(newName);
        this.close();
      } else {
        input.inputEl.focus();
      }
    });
    const btnContainer = contentEl.createDiv();
    btnContainer.style.display = "flex";
    btnContainer.style.gap = "10px";
    btnContainer.style.marginTop = "16px";
    new import_obsidian9.ButtonComponent(btnContainer).setButtonText("\u0421\u043E\u0445\u0440\u0430\u043D\u0438\u0442\u044C").setCta().onClick(() => {
      const value = input.inputEl.value.trim() || select.value;
      this.onSave(value);
      this.close();
    });
    new import_obsidian9.ButtonComponent(btnContainer).setButtonText("\u041E\u0442\u043C\u0435\u043D\u0430").onClick(() => this.close());
  }
  async addToTeam(name) {
    const routinesFile = this.app.vault.getAbstractFileByPath("routines.md");
    if (!routinesFile || !(routinesFile instanceof import_obsidian9.TFile)) {
      const content = `# \u041A\u043E\u043C\u0430\u043D\u0434\u0430
- ${name}
`;
      await this.app.vault.create("routines.md", content);
    } else {
      const content = await this.app.vault.read(routinesFile);
      if (!content.includes("# \u041A\u043E\u043C\u0430\u043D\u0434\u0430") && !content.includes("# \u043A\u043E\u043C\u0430\u043D\u0434\u0430")) {
        const newContent = content + "\n# \u041A\u043E\u043C\u0430\u043D\u0434\u0430\n- " + name + "\n";
        await this.app.vault.modify(routinesFile, newContent);
      } else if (!content.includes("- " + name)) {
        const lines = content.split("\n");
        let inTeamSection = false;
        const newLines = [];
        for (const line of lines) {
          if (line.trim().toLowerCase() === "# \u043A\u043E\u043C\u0430\u043D\u0434\u0430") {
            inTeamSection = true;
          }
          if (inTeamSection && (line.startsWith("# ") || line.startsWith("## "))) {
            newLines.push(line);
            newLines.push("- " + name);
            inTeamSection = false;
            continue;
          }
          newLines.push(line);
        }
        if (inTeamSection) {
          newLines.push("- " + name);
        }
        await this.app.vault.modify(routinesFile, newLines.join("\n"));
      }
    }
    this.onSave(name);
  }
};

// src/main-page/CalendarView.ts
var CalendarView = class extends BaseView {
  constructor(app, tasks, projects, weekOffset, onRefresh, setWeekOffset) {
    super(app);
    this.tasks = tasks;
    this.projects = projects;
    this.weekOffset = weekOffset;
    this.onRefresh = onRefresh;
    this.setWeekOffset = setWeekOffset;
  }
  render(container) {
    const wrapper = container.createDiv({ cls: "monitoring-calendar-wrapper" });
    this.renderNavigation(wrapper);
    this.renderWeekHeader(wrapper);
    this.renderDayHeaders(wrapper);
    this.renderTimelines(wrapper);
  }
  renderNavigation(wrapper) {
    const navHeader = wrapper.createDiv({ cls: "calendar-week-nav" });
    navHeader.createEl("button", {
      text: "\u2190 \u041F\u0440\u0435\u0434. \u043D\u0435\u0434\u0435\u043B\u044F",
      cls: "monitoring-refresh-btn"
    }).onclick = () => {
      this.weekOffset--;
      this.setWeekOffset(this.weekOffset);
      this.onRefresh();
    };
    navHeader.createEl("button", {
      text: "\u0421\u0435\u0433\u043E\u0434\u043D\u044F",
      cls: "monitoring-glass-btn"
    }).onclick = () => {
      this.weekOffset = 0;
      this.setWeekOffset(0);
      this.onRefresh();
    };
    navHeader.createEl("button", {
      text: "\u0421\u043B\u0435\u0434. \u043D\u0435\u0434\u0435\u043B\u044F \u2192",
      cls: "monitoring-refresh-btn"
    }).onclick = () => {
      this.weekOffset++;
      this.setWeekOffset(this.weekOffset);
      this.onRefresh();
    };
    const todayAt = new Date();
    const startOfWeek = this.getWeekStart(todayAt);
    const weekNum = this.getWeekNumber(startOfWeek);
    navHeader.createSpan({
      cls: "week-label",
      text: `\u041D\u0435\u0434\u0435\u043B\u044F ${weekNum}: ${startOfWeek.toLocaleDateString()} - ${this.getWeekEnd(startOfWeek).toLocaleDateString()}`
    });
  }
  renderWeekHeader(wrapper) {
    const daysHeader = wrapper.createDiv({ cls: "calendar-linear-header" });
    const weekDays = ["\u041F\u043D", "\u0412\u0442", "\u0421\u0440", "\u0427\u0442", "\u041F\u0442", "\u0421\u0431", "\u0412\u0441"];
    const datesInWeek = this.getDatesInWeek();
    datesInWeek.forEach((d, i) => {
      const dayHead = daysHeader.createDiv({ cls: "calendar-linear-day-head" });
      dayHead.createDiv({ cls: "day-name", text: weekDays[i] });
      dayHead.createDiv({ cls: "day-num", text: d.getDate().toString() });
      if (d.toDateString() === new Date().toDateString()) {
        dayHead.addClass("is-today");
      }
    });
  }
  renderDayHeaders(wrapper) {
  }
  renderTimelines(wrapper) {
    this.renderTimeline(wrapper, "\u041F\u0440\u043E\u0435\u043A\u0442\u044B (\u044D\u0442\u0430 \u043D\u0435\u0434\u0435\u043B\u044F)", this.projects, "project");
    this.renderTimeline(wrapper, "\u0417\u0430\u0434\u0430\u0447\u0438 (\u044D\u0442\u0430 \u043D\u0435\u0434\u0435\u043B\u044F)", this.tasks, "task");
  }
  renderTimeline(wrapper, title, items, type) {
    wrapper.createEl("h3", { text: title, attr: { style: "margin-top: 30px;" } });
    const timeline = wrapper.createDiv({ cls: "calendar-linear-timeline" });
    const grid = timeline.createDiv({ cls: "timeline-grid" });
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    const startOfWeek = this.getWeekStart(new Date());
    const todayCol = Math.floor((today.getTime() - startOfWeek.getTime()) / (1e3 * 60 * 60 * 24));
    for (let i = 0; i < 7; i++) {
      const line = grid.createDiv({ cls: "timeline-line" });
      if (i === todayCol && todayCol >= 0 && todayCol < 7) {
        line.addClass("timeline-line-today");
      }
    }
    const entriesContainer = timeline.createDiv({ cls: "timeline-entries" });
    const weekEnd = this.getWeekEnd(startOfWeek);
    items.forEach((item) => {
      const { start, end } = this.getItemDates(item, type);
      if (!start)
        return;
      const actualEnd = end || start;
      if (actualEnd < startOfWeek || start > weekEnd)
        return;
      const visibleStart = start < startOfWeek ? startOfWeek : start;
      const visibleEnd = actualEnd > weekEnd ? weekEnd : actualEnd;
      const { startCol, span } = this.calculatePosition(visibleStart, visibleEnd, startOfWeek);
      if (span <= 0)
        return;
      const entry = entriesContainer.createDiv({
        cls: "timeline-entry " + type + "-entry " + this.getStatusClass(item.status) + (type === "task" ? " priority-" + item.priority : ""),
        attr: {
          style: "grid-column: " + (startCol + 1) + " / span " + span,
          title: this.getEntryTooltip(item, type, start, actualEnd)
        }
      });
      const label = entry.createDiv({ cls: "entry-label" });
      label.createSpan({ text: item.name });
      if (item.responsible) {
        const meta = entry.createDiv({ cls: "entry-meta" });
        meta.createSpan({ text: "\u{1F464} " + item.responsible });
      }
      entry.onclick = () => this.openFile(item.path);
    });
  }
  getWeekStart(date) {
    const startOfWeek = new Date(date);
    const dayOfWeek = date.getDay();
    const diff = date.getDate() - dayOfWeek + (dayOfWeek === 0 ? -6 : 1) + this.weekOffset * 7;
    startOfWeek.setDate(diff);
    startOfWeek.setHours(0, 0, 0, 0);
    return startOfWeek;
  }
  getWeekEnd(startOfWeek) {
    const weekEnd = new Date(startOfWeek);
    weekEnd.setDate(startOfWeek.getDate() + 6);
    weekEnd.setHours(23, 59, 59, 999);
    return weekEnd;
  }
  getWeekNumber(date) {
    const startOfYear = new Date(date.getFullYear(), 0, 1);
    const days = Math.floor((date.getTime() - startOfYear.getTime()) / (24 * 60 * 60 * 1e3));
    const weekNum = Math.ceil((days + startOfYear.getDay() + 1) / 7);
    return weekNum;
  }
  getDatesInWeek() {
    const startOfWeek = this.getWeekStart(new Date());
    const dates = [];
    for (let i = 0; i < 7; i++) {
      const d = new Date(startOfWeek);
      d.setDate(startOfWeek.getDate() + i);
      dates.push(d);
    }
    return dates;
  }
  getItemDates(item, type) {
    if (type === "task") {
      const deadlineStr = item.deadline || "";
      if (deadlineStr.includes(" to ")) {
        const parts = deadlineStr.split(" to ");
        return {
          start: new Date(parts[0].trim()),
          end: new Date(parts[1].split(" ")[0].trim())
        };
      } else if (deadlineStr) {
        const date = new Date(deadlineStr.split(" ")[0].trim());
        return { start: date, end: date };
      }
      return { start: null, end: null };
    } else {
      const startStr = item.started;
      if (!startStr)
        return { start: null, end: null };
      const start = new Date(startStr);
      const endStr = item.target_date || item.deadline;
      const end = endStr ? new Date(endStr.split(" ")[0].trim()) : new Date(start);
      return { start, end };
    }
  }
  calculatePosition(visibleStart, visibleEnd, weekStart) {
    const normStart = new Date(visibleStart);
    normStart.setHours(0, 0, 0, 0);
    const normEnd = new Date(visibleEnd);
    normEnd.setHours(0, 0, 0, 0);
    const normWeekStart = new Date(weekStart);
    normWeekStart.setHours(0, 0, 0, 0);
    const startCol = Math.max(0, Math.floor((normStart.getTime() - normWeekStart.getTime()) / (1e3 * 60 * 60 * 24)));
    const duration = Math.ceil((normEnd.getTime() - normStart.getTime()) / (1e3 * 60 * 60 * 24)) + 1;
    const span = Math.min(7 - startCol, duration);
    return { startCol, span };
  }
  getEntryTooltip(item, type, start, end) {
    if (type === "project") {
      return "\u041F\u0440\u043E\u0435\u043A\u0442: " + item.name + "\n\u0426\u0435\u043B\u044C: " + (item.goal || "\u043D\u0435 \u0437\u0430\u0434\u0430\u043D\u0430") + "\n\u0421\u0442\u0430\u0442\u0443\u0441: " + item.status + "\n\u041F\u0435\u0440\u0438\u043E\u0434: " + start.toLocaleDateString() + " - " + end.toLocaleDateString();
    } else {
      return "\u0417\u0430\u0434\u0430\u0447\u0430: " + item.name + "\n\u0421\u0440\u043E\u043A: " + (item.deadline || "\u043D\u0435 \u0443\u043A\u0430\u0437\u0430\u043D") + "\n\u041F\u0440\u0438\u043E\u0440\u0438\u0442\u0435\u0442: " + item.priority + "\n\u0421\u0442\u0430\u0442\u0443\u0441: " + item.status;
    }
  }
};

// src/main-page/NotesView.ts
var NotesView = class extends BaseView {
  constructor(app, notes) {
    super(app);
    this.notes = notes;
  }
  render(container) {
    container.createEl("h3", { text: "\u041C\u043E\u0438 \u0417\u0430\u043C\u0435\u0442\u043A\u0438" });
    if (this.notes.length === 0) {
      this.createEmptyState(container, "\u0417\u0430\u043C\u0435\u0442\u043E\u043A \u043F\u043E\u043A\u0430 \u043D\u0435\u0442.");
      return;
    }
    const grid = container.createDiv({ cls: "monitoring-notes-grid" });
    this.notes.forEach((note) => {
      const card = grid.createDiv({ cls: "monitoring-note-card" });
      card.createDiv({ cls: "note-card-title", text: note.name });
      const meta = card.createDiv({ cls: "note-card-meta" });
      meta.createSpan({ text: "\u0421\u043E\u0437\u0434\u0430\u043D\u0430: " + (note.created || "---") });
      if (note.author) {
        meta.createSpan({ text: " | \u0410\u0432\u0442\u043E\u0440: " + note.author });
      }
      const tagsCont = card.createDiv({ cls: "note-card-tags" });
      note.tags.filter((t) => t !== "note").forEach((tag) => {
        tagsCont.createSpan({ cls: "monitoring-tag-pill", text: "#" + tag });
      });
      card.onclick = () => this.openFile(note.path);
    });
  }
};

// src/main-page/ResourcesView.ts
var import_obsidian10 = require("obsidian");
var ResourcesView = class extends BaseView {
  constructor(app) {
    super(app);
    this.groups = [];
  }
  async render(container) {
    container.addClass("monitoring-resources-view");
    this.groups = await this.parseResourcesFile();
    const header = container.createDiv({ cls: "resources-header" });
    header.createEl("h2", { text: "\u0420\u0435\u0441\u0443\u0440\u0441\u044B", cls: "resources-title" });
    header.createEl("button", {
      cls: "monitoring-glass-btn resources-add-group-btn",
      text: "+ \u0413\u0440\u0443\u043F\u043F\u0443"
    }).onclick = () => this.showAddGroupModal();
    const content = container.createDiv({ cls: "resources-content" });
    if (this.groups.length === 0) {
      content.createDiv({
        cls: "empty-state-text",
        text: '\u0420\u0435\u0441\u0443\u0440\u0441\u044B \u043D\u0435 \u043D\u0430\u0439\u0434\u0435\u043D\u044B. \u041D\u0430\u0436\u043C\u0438\u0442\u0435 "+ \u0413\u0440\u0443\u043F\u043F\u0443" \u0447\u0442\u043E\u0431\u044B \u0441\u043E\u0437\u0434\u0430\u0442\u044C \u043F\u0435\u0440\u0432\u0443\u044E.'
      });
      return;
    }
    for (const group of this.groups) {
      this.renderGroup(content, group);
    }
  }
  renderGroup(container, group) {
    const groupEl = container.createDiv({ cls: "resource-group" });
    const groupHeader = groupEl.createDiv({ cls: "resource-group-header" });
    const groupTitle = groupHeader.createDiv({ cls: "resource-group-title" });
    if (group.icon) {
      groupTitle.createSpan({ text: group.icon + " ", cls: "group-icon" });
    }
    groupTitle.createSpan({ text: group.name });
    groupHeader.createEl("button", {
      cls: "resource-add-btn",
      text: "+"
    }).onclick = () => this.showAddResourceModal(group.name, group.icon);
    const groupItems = groupEl.createDiv({ cls: "resource-group-items" });
    if (group.items.length === 0) {
      groupItems.createDiv({ text: "\u041D\u0435\u0442 \u0440\u0435\u0441\u0443\u0440\u0441\u043E\u0432", cls: "empty-state-text" });
      return;
    }
    for (const item of group.items) {
      this.renderResourceItem(groupItems, item, group.name);
    }
  }
  renderResourceItem(container, item, groupName) {
    const btn = container.createEl("button", {
      cls: "resource-item",
      text: `${item.icon} ${item.name}`
    });
    btn.onclick = (e) => {
      e.preventDefault();
      e.stopPropagation();
      if (item.url.startsWith("http") || item.url.startsWith("//")) {
        window.open(item.url, "_blank");
      } else {
        this.openInExplorer(item.url);
      }
    };
    btn.oncontextmenu = (e) => {
      e.preventDefault();
      this.showResourceContextMenu(e, item, groupName);
    };
  }
  openInExplorer(path2) {
    let fullPath;
    if (/^[A-Za-z]:/.test(path2)) {
      fullPath = path2;
    } else {
      const vaultPath = this.app.vault.adapter.basePath;
      if (!vaultPath) {
        new import_obsidian10.Notice("\u041D\u0435 \u0443\u0434\u0430\u043B\u043E\u0441\u044C \u043E\u0442\u043A\u0440\u044B\u0442\u044C \u043F\u0440\u043E\u0432\u043E\u0434\u043D\u0438\u043A");
        return;
      }
      fullPath = `${vaultPath}\\${path2.replace(/\//g, "\\")}`;
    }
    window.open(`file://${fullPath.replace(/\\/g, "/")}`);
  }
  showResourceContextMenu(e, item, groupName) {
    const menu = document.createElement("div");
    menu.className = "context-menu";
    menu.style.position = "fixed";
    menu.style.left = `${e.clientX}px`;
    menu.style.top = `${e.clientY}px`;
    menu.style.zIndex = "1000";
    const deleteBtn = menu.createDiv({
      cls: "context-menu-item delete-item",
      text: "\u0423\u0434\u0430\u043B\u0438\u0442\u044C"
    });
    deleteBtn.onclick = () => {
      this.deleteResource(groupName, item);
      menu.remove();
    };
    document.body.appendChild(menu);
    const closeMenu = (event) => {
      if (!menu.contains(event.target)) {
        menu.remove();
        document.removeEventListener("click", closeMenu);
      }
    };
    setTimeout(() => document.addEventListener("click", closeMenu), 10);
  }
  async deleteResource(groupName, item) {
    const file = this.app.vault.getAbstractFileByPath("resources.md");
    if (!file)
      return;
    let content = await this.app.vault.read(file);
    const lines = content.split("\n");
    const newLines = [];
    let inGroup = false;
    let skipNextLine = false;
    for (let i = 0; i < lines.length; i++) {
      const line = lines[i];
      if (line.match(/^##\s/) && line.includes(groupName)) {
        inGroup = true;
        newLines.push(line);
        continue;
      }
      if (inGroup && line.match(/^##\s/)) {
        inGroup = false;
      }
      if (inGroup && line.includes(`[${item.icon}](${item.url})`)) {
        skipNextLine = true;
        continue;
      }
      if (skipNextLine && line.trim() === "") {
        skipNextLine = false;
        continue;
      }
      newLines.push(line);
    }
    await this.app.vault.modify(file, newLines.join("\n"));
    new import_obsidian10.Notice("\u0420\u0435\u0441\u0443\u0440\u0441 \u0443\u0434\u0430\u043B\u0451\u043D");
    this.render(this.containerEl.children[1]);
  }
  showAddGroupModal() {
    new AddGroupModal(this.app, async (name, icon) => {
      await this.addGroup(name, icon);
      this.render(this.containerEl.children[1]);
    }).open();
  }
  showAddResourceModal(groupName, groupIcon = "\u2699\uFE0F") {
    new AddResourceModal(this.app, async (name, url, icon) => {
      await this.addResource(groupName, groupIcon, name, url, icon);
      this.render(this.containerEl.children[1]);
    }).open();
  }
  async addGroup(name, icon) {
    let file = this.app.vault.getAbstractFileByPath("resources.md");
    let content = "";
    if (file) {
      content = await this.app.vault.read(file);
      content += "\n\n";
    }
    content += `## ${icon} ${name}`;
    if (file) {
      await this.app.vault.modify(file, content);
    } else {
      await this.app.vault.create("resources.md", `# \u0420\u0435\u0441\u0443\u0440\u0441\u044B

${content}`);
    }
    new import_obsidian10.Notice(`\u0413\u0440\u0443\u043F\u043F\u0430 "${name}" \u0441\u043E\u0437\u0434\u0430\u043D\u0430`);
  }
  async addResource(groupName, groupIcon, name, url, icon) {
    let file = this.app.vault.getAbstractFileByPath("resources.md");
    if (!file) {
      await this.app.vault.create("resources.md", `# \u0420\u0435\u0441\u0443\u0440\u0441\u044B

## ${groupIcon} ${groupName}
- [${icon}](${url}) ${name}`);
      new import_obsidian10.Notice(`\u0420\u0435\u0441\u0443\u0440\u0441 "${name}" \u0434\u043E\u0431\u0430\u0432\u043B\u0435\u043D`);
      this.render(this.containerEl.children[1]);
      return;
    }
    let content = await this.app.vault.read(file);
    const lines = content.split("\n");
    let insertIndex = -1;
    for (let i = 0; i < lines.length; i++) {
      const line = lines[i].trim();
      if (line.startsWith("##")) {
        const headerText = line.replace(/^##\s*/, "").trim();
        const parts = headerText.split(/\s+/);
        const actualName = parts.slice(parts[0].match(/^[^\w\s]/) ? 1 : 0).join(" ");
        if (actualName === groupName) {
          insertIndex = i;
          break;
        }
      }
    }
    if (insertIndex === -1) {
      new import_obsidian10.Notice("\u0413\u0440\u0443\u043F\u043F\u0430 \u043D\u0435 \u043D\u0430\u0439\u0434\u0435\u043D\u0430: " + groupName);
      return;
    }
    let insertPos = insertIndex + 1;
    while (insertPos < lines.length && lines[insertPos].trim() !== "" && !lines[insertPos].trim().startsWith("##")) {
      insertPos++;
    }
    const newLine = `- [${icon}](${url}) ${name}`;
    lines.splice(insertPos, 0, newLine);
    await this.app.vault.modify(file, lines.join("\n"));
    new import_obsidian10.Notice(`\u0420\u0435\u0441\u0443\u0440\u0441 "${name}" \u0434\u043E\u0431\u0430\u0432\u043B\u0435\u043D`);
    this.render(this.containerEl.children[1]);
  }
  async parseResourcesFile() {
    const file = this.app.vault.getAbstractFileByPath("resources.md");
    if (!file)
      return this.getDefaultGroups();
    try {
      const content = await this.app.vault.read(file);
      return this.parseResourcesContent(content);
    } catch (e) {
      console.error("Error reading resources.md:", e);
      return this.getDefaultGroups();
    }
  }
  getDefaultGroups() {
    return [
      { name: "\u0410\u0434\u043C\u0438\u043D\u0438\u0441\u0442\u0440\u0438\u0440\u043E\u0432\u0430\u043D\u0438\u0435", icon: "\u2699\uFE0F", items: [] },
      { name: "\u0420\u0430\u0437\u0440\u0430\u0431\u043E\u0442\u043A\u0430", icon: "\u{1F527}", items: [] },
      { name: "\u0421\u043F\u0440\u0430\u0432\u043E\u0447\u043D\u044B\u0435 \u043C\u0430\u0442\u0435\u0440\u0438\u0430\u043B\u044B", icon: "\u{1F4DA}", items: [] }
    ];
  }
  parseResourcesContent(content) {
    var _a;
    const groups = [];
    const lines = content.split("\n");
    let currentGroup = null;
    for (const line of lines) {
      const groupMatch = line.match(/^##\s*(.+)$/);
      if (groupMatch) {
        if (currentGroup) {
          groups.push(currentGroup);
        }
        const parts = groupMatch[1].trim().split(/\s+/);
        const icon = parts[0].match(/^[^\w\s]/) ? parts[0] : "";
        const name = icon ? parts.slice(1).join(" ") : groupMatch[1].trim();
        currentGroup = { name, icon, items: [] };
        continue;
      }
      const itemMatch = line.match(/^-\s*\[([^\]]+)\]\(([^)]+)\)\s*(.+)?$/);
      if (itemMatch && currentGroup) {
        currentGroup.items.push({
          icon: itemMatch[1],
          url: itemMatch[2],
          name: ((_a = itemMatch[3]) == null ? void 0 : _a.trim()) || itemMatch[2]
        });
      }
    }
    if (currentGroup) {
      groups.push(currentGroup);
    }
    return groups.length > 0 ? groups : this.getDefaultGroups();
  }
  get containerEl() {
    var _a, _b;
    return ((_b = (_a = this.app.workspace.getLeavesOfType("markdown")[0]) == null ? void 0 : _a.view) == null ? void 0 : _b.containerEl) || document.body;
  }
};
var ICON_LIST = ["\u{1F4C1}", "\u{1F4C2}", "\u2699\uFE0F", "\u{1F527}", "\u{1F517}", "\u{1F310}", "\u{1F4DA}", "\u{1F4DD}", "\u{1F4CA}", "\u{1F4C8}", "\u{1F4BC}", "\u{1F3E0}", "\u{1F3AF}", "\u{1F4CB}", "\u{1F5C2}\uFE0F", "\u{1F5A5}\uFE0F", "\u{1F4F1}", "\u2601\uFE0F", "\u{1F512}", "\u{1F6E0}\uFE0F"];
var AddGroupModal = class extends import_obsidian10.Modal {
  constructor(app, onSubmit) {
    super(app);
    this.selectedIcon = "\u{1F4C1}";
    this.onSubmit = onSubmit;
  }
  onOpen() {
    var _a;
    const { contentEl } = this;
    contentEl.createEl("h3", { text: "\u0414\u043E\u0431\u0430\u0432\u0438\u0442\u044C \u0433\u0440\u0443\u043F\u043F\u0443" });
    const iconContainer = contentEl.createDiv({ cls: "icon-picker-container" });
    iconContainer.createDiv({ text: "\u0412\u044B\u0431\u0435\u0440\u0438\u0442\u0435 \u0438\u043A\u043E\u043D\u043A\u0443:", cls: "icon-picker-label" });
    const iconGrid = iconContainer.createDiv({ cls: "icon-picker-grid" });
    ICON_LIST.forEach((icon) => {
      const iconBtn = iconGrid.createEl("button", { text: icon, cls: "icon-picker-btn" });
      iconBtn.onclick = () => {
        this.selectedIcon = icon;
        iconGrid.querySelectorAll(".icon-picker-btn").forEach((b) => b.removeClass("selected"));
        iconBtn.addClass("selected");
      };
    });
    (_a = iconGrid.querySelector("button")) == null ? void 0 : _a.addClass("selected");
    const nameInput = new import_obsidian10.TextComponent(contentEl);
    nameInput.setPlaceholder("\u041D\u0430\u0437\u0432\u0430\u043D\u0438\u0435 \u0433\u0440\u0443\u043F\u043F\u044B");
    nameInput.inputEl.style.width = "100%";
    nameInput.inputEl.style.marginBottom = "20px";
    new import_obsidian10.ButtonComponent(contentEl).setButtonText("\u0414\u043E\u0431\u0430\u0432\u0438\u0442\u044C").setCta().onClick(() => {
      const name = nameInput.getValue().trim();
      if (name) {
        this.onSubmit(name, this.selectedIcon);
        this.close();
      }
    });
  }
};
var AddResourceModal = class extends import_obsidian10.Modal {
  constructor(app, onSubmit) {
    super(app);
    this.selectedIcon = "\u{1F517}";
    this.onSubmit = onSubmit;
  }
  onOpen() {
    var _a;
    const { contentEl } = this;
    contentEl.createEl("h3", { text: "\u0414\u043E\u0431\u0430\u0432\u0438\u0442\u044C \u0440\u0435\u0441\u0443\u0440\u0441" });
    const iconContainer = contentEl.createDiv({ cls: "icon-picker-container" });
    iconContainer.createDiv({ text: "\u0412\u044B\u0431\u0435\u0440\u0438\u0442\u0435 \u0438\u043A\u043E\u043D\u043A\u0443:", cls: "icon-picker-label" });
    const iconGrid = iconContainer.createDiv({ cls: "icon-picker-grid" });
    ICON_LIST.forEach((icon) => {
      const iconBtn = iconGrid.createEl("button", { text: icon, cls: "icon-picker-btn" });
      iconBtn.onclick = () => {
        this.selectedIcon = icon;
        iconGrid.querySelectorAll(".icon-picker-btn").forEach((b) => b.removeClass("selected"));
        iconBtn.addClass("selected");
      };
    });
    (_a = iconGrid.querySelector("button")) == null ? void 0 : _a.addClass("selected");
    const nameInput = new import_obsidian10.TextComponent(contentEl);
    nameInput.setPlaceholder("\u041D\u0430\u0437\u0432\u0430\u043D\u0438\u0435");
    nameInput.inputEl.style.width = "100%";
    nameInput.inputEl.style.marginBottom = "10px";
    const urlInput = new import_obsidian10.TextComponent(contentEl);
    urlInput.setPlaceholder("URL \u0438\u043B\u0438 \u043F\u0443\u0442\u044C \u043A \u043F\u0430\u043F\u043A\u0435 (https://... \u0438\u043B\u0438 C:\\...)");
    urlInput.inputEl.style.width = "100%";
    urlInput.inputEl.style.marginBottom = "20px";
    new import_obsidian10.ButtonComponent(contentEl).setButtonText("\u0414\u043E\u0431\u0430\u0432\u0438\u0442\u044C").setCta().onClick(() => {
      const name = nameInput.getValue().trim();
      const url = urlInput.getValue().trim();
      if (name && url) {
        this.onSubmit(name, url, this.selectedIcon);
        this.close();
      }
    });
  }
};

// src/main-page/MainPageView.ts
var MAIN_PAGE_VIEW_TYPE = "monitoring-main-page-view";
var MainPageView = class extends import_obsidian11.ItemView {
  constructor(leaf, plugin) {
    super(leaf);
    this.activeTab = "dashboard";
    this.calendarWeekOffset = 0;
    this.currentFilterId = null;
    this.selectedResponsible = null;
    this.teamMembers = [];
    this.isRefreshing = false;
    this.plugin = plugin;
    this.dataService = new DataService(
      this.app,
      this.plugin.settings.incidentsFolder,
      this.plugin.settings.simpleNotesFolder
    );
    this.teamService = new TeamService(this.app);
    this.resourcesView = new ResourcesView(this.app);
  }
  getViewType() {
    return MAIN_PAGE_VIEW_TYPE;
  }
  getDisplayText() {
    return "\u041F\u0430\u043D\u0435\u043B\u044C \u0443\u043F\u0440\u0430\u0432\u043B\u0435\u043D\u0438\u044F";
  }
  getIcon() {
    return "brain";
  }
  async onOpen() {
    await this.refreshContent();
  }
  async refreshContent() {
    if (this.isRefreshing)
      return;
    if (!this.containerEl.parentElement)
      return;
    this.isRefreshing = true;
    try {
      this.teamMembers = await this.teamService.getTeamMembers();
      const container = this.containerEl.children[1];
      container.empty();
      container.addClass("monitoring-main-page");
      this.renderHeader(container);
      this.renderFilterBar(container);
      this.renderTabs(container);
      let data = await this.dataService.fetchVaultData();
      if (this.selectedResponsible) {
        data = {
          ...data,
          tasks: data.tasks.filter((t) => t.responsible === this.selectedResponsible),
          projects: data.projects.filter((p) => p.responsible === this.selectedResponsible)
        };
      }
      if (this.activeTab === "dashboard") {
        const dashboardView = new DashboardView(
          this.app,
          this.dataService,
          data.incidents,
          data.tasks,
          data.projects,
          this.plugin.settings.savedFilters || [],
          this.currentFilterId,
          this.plugin.settings.dashboardNoteName
        );
        dashboardView.render(container);
      } else if (this.activeTab === "kanban") {
        const kanbanView = new KanbanView(
          this.app,
          this.dataService,
          this.plugin.templateManager,
          data.tasks,
          data.projects,
          () => this.refreshContent(),
          this.teamMembers,
          this.plugin.settings.currentUser
        );
        kanbanView.render(container);
      } else if (this.activeTab === "calendar") {
        const calendarView = new CalendarView(
          this.app,
          data.tasks,
          data.projects,
          this.calendarWeekOffset,
          () => this.refreshContent(),
          (offset) => {
            this.calendarWeekOffset = offset;
          }
        );
        calendarView.render(container);
      } else if (this.activeTab === "notes") {
        const notesView = new NotesView(this.app, data.notes);
        notesView.render(container);
      } else if (this.activeTab === "resources") {
        this.resourcesView.render(container);
      }
    } finally {
      this.isRefreshing = false;
    }
  }
  renderHeader(container) {
    var _a;
    if (container.querySelector(".main-page-header-container"))
      return;
    const headerContainer = container.createDiv({ cls: "main-page-header-container" });
    const version = ((_a = this.plugin.manifest) == null ? void 0 : _a.version) || "latest";
    headerContainer.createEl("h2", { text: "\u041F\u0430\u043D\u0435\u043B\u044C \u0443\u043F\u0440\u0430\u0432\u043B\u0435\u043D\u0438\u044F v" + version, cls: "main-page-header" });
    const btnGroup = headerContainer.createDiv({ cls: "monitoring-header-btns" });
    btnGroup.createEl("button", {
      cls: "monitoring-refresh-btn",
      text: "\u041E\u0431\u043D\u043E\u0432\u0438\u0442\u044C \u0434\u0430\u043D\u043D\u044B\u0435"
    }).onclick = () => this.refreshContent();
    btnGroup.createEl("button", {
      cls: "monitoring-report-btn",
      text: "\u0421\u0444\u043E\u0440\u043C\u0438\u0440\u043E\u0432\u0430\u0442\u044C \u043E\u0442\u0447\u0435\u0442"
    }).onclick = () => this.generateWeeklyReport();
    btnGroup.createEl("button", {
      cls: "monitoring-glass-btn monitoring-add-task",
      text: "+ \u0417\u0430\u0434\u0430\u0447\u0443"
    }).onclick = () => this.showNamingModal("\u0421\u043E\u0437\u0434\u0430\u0442\u044C \u043D\u043E\u0432\u0443\u044E \u0437\u0430\u0434\u0430\u0447\u0443", async (name) => {
      const file = await this.plugin.templateManager.createTaskNote(name);
      await this.app.workspace.getLeaf(false).openFile(file);
      new import_obsidian11.Notice('\u0417\u0430\u0434\u0430\u0447\u0430 "' + name + '" \u0441\u043E\u0437\u0434\u0430\u043D\u0430!');
      this.refreshContent();
    });
    btnGroup.createEl("button", {
      cls: "monitoring-glass-btn monitoring-add-project",
      text: "+ \u041F\u0440\u043E\u0435\u043A\u0442"
    }).onclick = () => this.showNamingModal("\u0421\u043E\u0437\u0434\u0430\u0442\u044C \u043D\u043E\u0432\u044B\u0439 \u043F\u0440\u043E\u0435\u043A\u0442", async (name) => {
      const file = await this.plugin.templateManager.createProjectNote(name);
      await this.app.workspace.getLeaf(false).openFile(file);
      new import_obsidian11.Notice('\u041F\u0440\u043E\u0435\u043A\u0442 "' + name + '" \u0441\u043E\u0437\u0434\u0430\u043D!');
      this.refreshContent();
    });
    btnGroup.createEl("button", {
      cls: "monitoring-glass-btn monitoring-add-note",
      text: "+ \u0417\u0430\u043C\u0435\u0442\u043A\u0443"
    }).onclick = () => this.showNamingModal("\u0421\u043E\u0437\u0434\u0430\u0442\u044C \u043D\u043E\u0432\u0443\u044E \u0437\u0430\u043C\u0435\u0442\u043A\u0443", async (name) => {
      const file = await this.plugin.templateManager.createSimpleNote(name);
      await this.app.workspace.getLeaf(false).openFile(file);
      new import_obsidian11.Notice('\u0417\u0430\u043C\u0435\u0442\u043A\u0430 "' + name + '" \u0441\u043E\u0437\u0434\u0430\u043D\u0430!');
      this.refreshContent();
    });
    btnGroup.createEl("button", {
      cls: "monitoring-glass-btn monitoring-daily-btn",
      text: "\u{1F4C5} Daily"
    }).onclick = async () => {
      try {
        const file = await this.plugin.dailyService.createDailyNote();
        await this.app.workspace.getLeaf(false).openFile(file);
        new import_obsidian11.Notice("\u0415\u0436\u0435\u0434\u043D\u0435\u0432\u043D\u0430\u044F \u0437\u0430\u043C\u0435\u0442\u043A\u0430 \u043E\u0442\u043A\u0440\u044B\u0442\u0430!");
      } catch (e) {
        new import_obsidian11.Notice("\u041E\u0448\u0438\u0431\u043A\u0430: " + e.message);
      }
    };
  }
  renderFilterBar(container) {
    const filterContainer = container.createDiv({ cls: "monitoring-filter-bar" });
    const label = filterContainer.createSpan({ text: "\u041E\u0442\u0432\u0435\u0442\u0441\u0442\u0432\u0435\u043D\u043D\u044B\u0439: " });
    label.style.marginRight = "10px";
    const select = filterContainer.createEl("select", { cls: "monitoring-filter-select" });
    select.style.padding = "6px 12px";
    select.style.borderRadius = "6px";
    select.style.backgroundColor = "var(--background-primary)";
    select.style.border = "1px solid var(--border-color)";
    select.style.color = "var(--text-normal)";
    const defaultOption = select.createEl("option");
    defaultOption.text = "\u0412\u0441\u0435";
    defaultOption.value = "";
    defaultOption.selected = !this.selectedResponsible;
    const currentUser = this.plugin.settings.currentUser;
    const allMembers = [...this.teamMembers];
    if (currentUser && !allMembers.includes(currentUser)) {
      allMembers.push(currentUser);
    }
    allMembers.forEach((member) => {
      const option = select.createEl("option");
      option.text = member;
      option.value = member;
      if (this.selectedResponsible === member) {
        option.selected = true;
      }
    });
    if (currentUser) {
      const myTasksOption = select.createEl("option");
      myTasksOption.text = "\u041C\u043E\u0438 \u0437\u0430\u0434\u0430\u0447\u0438";
      myTasksOption.value = "__my__";
      if (this.selectedResponsible === currentUser) {
        myTasksOption.selected = true;
      }
    }
    select.onchange = () => {
      const value = select.value;
      if (value === "__my__") {
        this.selectedResponsible = this.plugin.settings.currentUser || null;
      } else if (value === "") {
        this.selectedResponsible = null;
      } else {
        this.selectedResponsible = value;
      }
      this.refreshContent();
    };
  }
  renderTabs(container) {
    const tabsContainer = container.createDiv({ cls: "monitoring-tabs-nav" });
    const tabs = [
      { id: "dashboard", label: "\u0414\u0430\u0448\u0431\u043E\u0440\u0434" },
      { id: "kanban", label: "\u041A\u0430\u043D\u0431\u0430\u043D" },
      { id: "calendar", label: "\u041A\u0430\u043B\u0435\u043D\u0434\u0430\u0440\u044C" },
      { id: "notes", label: "\u0417\u0430\u043C\u0435\u0442\u043A\u0438" },
      { id: "resources", label: "\u0420\u0435\u0441\u0443\u0440\u0441\u044B" }
    ];
    tabs.forEach((tab) => {
      const tabEl = tabsContainer.createDiv({
        cls: "monitoring-tab-item " + (this.activeTab === tab.id ? "is-active" : "")
      });
      tabEl.createSpan({ text: tab.label });
      tabEl.onclick = (e) => {
        e.stopPropagation();
        e.preventDefault();
        this.activeTab = tab.id;
        this.refreshContent();
      };
    });
  }
  showNamingModal(title, onSubmit) {
    new NamingModal(this.app, title, onSubmit).open();
  }
  async generateWeeklyReport() {
    new import_obsidian11.Notice("\u0413\u0435\u043D\u0435\u0440\u0430\u0446\u0438\u044F \u043E\u0442\u0447\u0435\u0442\u0430 \u0437\u0430 \u043D\u0435\u0434\u0435\u043B\u044E...");
    try {
      const data = await this.dataService.fetchVaultData();
      if (data.incidents.length === 0) {
        new import_obsidian11.Notice("\u041D\u0435\u0442 \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u043E\u0432 \u0434\u043B\u044F \u0430\u043D\u0430\u043B\u0438\u0437\u0430.");
        return;
      }
      let combinedText = "";
      for (const inc of data.incidents.slice(0, 10)) {
        const file2 = this.app.vault.getAbstractFileByPath(inc.path);
        const content = await this.app.vault.read(file2);
        const summaryMatch = content.match(/## Текущее саммари инцидента\n([\s\S]*?)\n---/);
        if (summaryMatch) {
          combinedText += "--- Incident: " + inc.name + " ---\n" + summaryMatch[1] + "\n\n";
        }
      }
      if (!combinedText) {
        new import_obsidian11.Notice("\u041D\u0435 \u0443\u0434\u0430\u043B\u043E\u0441\u044C \u0438\u0437\u0432\u043B\u0435\u0447\u044C \u0434\u0430\u043D\u043D\u044B\u0435 \u0434\u043B\u044F \u043E\u0442\u0447\u0435\u0442\u0430.");
        return;
      }
      const report = await this.plugin.llmService.generateWeeklyReport(combinedText);
      const dateStr = new Date().toISOString().split("T")[0];
      const fileName = "Weekly-Report-" + dateStr + ".md";
      const fileContent = "# \u0415\u0436\u0435\u043D\u0435\u0434\u0435\u043B\u044C\u043D\u044B\u0439 \u043E\u0442\u0447\u0435\u0442 \u043E\u0442 " + dateStr + "\n\n" + report + "\n\n## \u041F\u0440\u043E\u0430\u043D\u0430\u043B\u0438\u0437\u0438\u0440\u043E\u0432\u0430\u043D\u043D\u044B\u0435 \u0438\u043D\u0446\u0438\u0434\u0435\u043D\u0442\u044B\n" + data.incidents.slice(0, 10).map((i) => "- [[" + i.path + "|" + i.name + "]]").join("\n");
      const file = await this.app.vault.create(fileName, fileContent);
      await this.app.workspace.getLeaf(false).openFile(file);
      new import_obsidian11.Notice("\u041E\u0442\u0447\u0435\u0442 \u0443\u0441\u043F\u0435\u0448\u043D\u043E \u0441\u043E\u0437\u0434\u0430\u043D!");
    } catch (error) {
      console.error(error);
      new import_obsidian11.Notice("\u041E\u0448\u0438\u0431\u043A\u0430 \u043F\u0440\u0438 \u0433\u0435\u043D\u0435\u0440\u0430\u0446\u0438\u0438 \u043E\u0442\u0447\u0435\u0442\u0430: " + error.message);
    }
  }
  async onClose() {
  }
};
var NamingModal = class extends import_obsidian11.Modal {
  constructor(app, title, onSubmit) {
    super(app);
    this.name = "";
    this.title = title;
    this.onSubmit = onSubmit;
  }
  submit() {
    if (this.name) {
      this.onSubmit(this.name);
      this.close();
    } else {
      new import_obsidian11.Notice("\u041F\u043E\u0436\u0430\u043B\u0443\u0439\u0441\u0442\u0430, \u0432\u0432\u0435\u0434\u0438\u0442\u0435 \u043D\u0430\u0437\u0432\u0430\u043D\u0438\u0435");
    }
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.createEl("h3", { text: this.title });
    const input = new import_obsidian11.TextComponent(contentEl);
    input.setPlaceholder("\u0412\u0432\u0435\u0434\u0438\u0442\u0435 \u043D\u0430\u0437\u0432\u0430\u043D\u0438\u0435...");
    input.onChange((value) => this.name = value);
    input.inputEl.classList.add("modal-input");
    input.inputEl.style.width = "100%";
    input.inputEl.style.marginBottom = "20px";
    requestAnimationFrame(() => {
      input.inputEl.focus();
    });
    setTimeout(() => {
      input.inputEl.focus();
    }, 100);
    const btnContainer = contentEl.createDiv({ cls: "modal-button-container" });
    new import_obsidian11.ButtonComponent(btnContainer).setButtonText("\u0421\u043E\u0437\u0434\u0430\u0442\u044C").setCta().onClick(() => this.submit());
    input.inputEl.addEventListener("keydown", (e) => {
      if (e.key === "Enter") {
        e.preventDefault();
        this.submit();
      }
    });
  }
  onClose() {
    const { contentEl } = this;
    contentEl.empty();
  }
};

// src/MonitoringDurationChild.ts
var import_obsidian17 = require("obsidian");

// src/modals/TagModal.ts
var import_obsidian12 = require("obsidian");
var TagModal = class extends import_obsidian12.Modal {
  constructor(app, onSubmit) {
    super(app);
    this.query = "";
    this.onSubmit = onSubmit;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.createEl("h3", { text: "\u0414\u043E\u0431\u0430\u0432\u0438\u0442\u044C \u0442\u0435\u0433" });
    const input = new import_obsidian12.TextComponent(contentEl);
    input.setPlaceholder("\u041D\u0430\u0447\u043D\u0438\u0442\u0435 \u0432\u0432\u043E\u0434\u0438\u0442\u044C \u043D\u0430\u0437\u0432\u0430\u043D\u0438\u0435...");
    input.inputEl.style.width = "100%";
    input.inputEl.focus();
    const suggestionContainer = contentEl.createDiv({ cls: "tag-suggestions" });
    const allTags = Object.keys(this.app.metadataCache.getTags()).map((t) => t.substring(1));
    const renderSuggestions = (query) => {
      suggestionContainer.empty();
      allTags.filter((t) => t.toLowerCase().includes(query.toLowerCase())).slice(0, 10).forEach((tag) => {
        const item = suggestionContainer.createDiv({ cls: "tag-suggestion-item", text: tag });
        item.onclick = () => {
          this.onSubmit(tag);
          this.close();
        };
      });
    };
    input.onChange((val) => {
      this.query = val;
      renderSuggestions(val);
    });
    input.inputEl.addEventListener("keydown", (e) => {
      if (e.key === "Enter") {
        e.preventDefault();
        if (this.query) {
          this.onSubmit(this.query);
          this.close();
        }
      }
    });
    renderSuggestions("");
  }
  onClose() {
    this.contentEl.empty();
  }
};

// src/modals/ResourceModal.ts
var import_obsidian13 = require("obsidian");
var ResourceModal = class extends import_obsidian13.Modal {
  constructor(app, onSubmit) {
    super(app);
    this.link = "";
    this.description = "";
    this.onSubmit = onSubmit;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.createEl("h3", { text: "\u0414\u043E\u0431\u0430\u0432\u0438\u0442\u044C \u043D\u043E\u0432\u044B\u0439 \u0440\u0435\u0441\u0443\u0440\u0441" });
    const lInput = new import_obsidian13.TextComponent(contentEl);
    lInput.setPlaceholder("http://...");
    lInput.onChange((val) => this.link = val);
    lInput.inputEl.style.width = "100%";
    const dInput = new import_obsidian13.TextComponent(contentEl);
    dInput.setPlaceholder("\u041E\u043F\u0438\u0441\u0430\u043D\u0438\u0435...");
    dInput.onChange((val) => this.description = val);
    dInput.inputEl.style.width = "100%";
    new import_obsidian13.ButtonComponent(contentEl.createDiv({ cls: "modal-button-container" })).setButtonText("\u0414\u043E\u0431\u0430\u0432\u0438\u0442\u044C").setCta().onClick(() => {
      if (this.link) {
        this.onSubmit(this.link, this.description);
        this.close();
      }
    });
  }
  onClose() {
    this.contentEl.empty();
  }
};

// src/modals/NewTaskModal.ts
var import_obsidian14 = require("obsidian");
var NewTaskModal = class extends import_obsidian14.Modal {
  constructor(app, onSubmit) {
    super(app);
    this.taskName = "";
    this.onSubmit = onSubmit;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.createEl("h3", { text: "\u0421\u043E\u0437\u0434\u0430\u0442\u044C \u043D\u043E\u0432\u0443\u044E \u0437\u0430\u0434\u0430\u0447\u0443" });
    const input = new import_obsidian14.TextComponent(contentEl);
    input.setPlaceholder("\u041D\u0430\u0437\u0432\u0430\u043D\u0438\u0435...");
    input.inputEl.style.width = "100%";
    input.onChange((val) => this.taskName = val);
    requestAnimationFrame(() => {
      input.inputEl.focus();
    });
    new import_obsidian14.ButtonComponent(contentEl.createDiv({ cls: "modal-button-container" })).setButtonText("\u0421\u043E\u0437\u0434\u0430\u0442\u044C").setCta().onClick(() => {
      if (this.taskName) {
        this.onSubmit(this.taskName);
        this.close();
      }
    });
  }
  onClose() {
    this.contentEl.empty();
  }
};

// src/modals/ResponsibleButtonModal.ts
var import_obsidian15 = require("obsidian");
var ResponsibleButtonModal = class extends import_obsidian15.Modal {
  constructor(app, file, onSave) {
    super(app);
    this.teamMembers = [];
    this.currentUser = "";
    this.file = file;
    this.onSave = onSave;
    this.teamService = new TeamService(app);
  }
  async onOpen() {
    var _a, _b;
    const { contentEl } = this;
    contentEl.createEl("h3", { text: "\u0412\u044B\u0431\u0440\u0430\u0442\u044C \u043E\u0442\u0432\u0435\u0442\u0441\u0442\u0432\u0435\u043D\u043D\u043E\u0433\u043E" });
    this.teamMembers = await this.teamService.getTeamMembers();
    const cache = this.app.metadataCache.getFileCache(this.file);
    this.currentUser = this.settings.currentUser || "";
    const allMembers = [...this.teamMembers];
    if (this.currentUser && !allMembers.includes(this.currentUser)) {
      allMembers.push(this.currentUser);
    }
    const selectContainer = contentEl.createDiv();
    selectContainer.style.marginBottom = "20px";
    const select = selectContainer.createEl("select");
    select.style.width = "100%";
    select.style.padding = "8px";
    const currentValue = ((_a = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _a["responsible"]) || ((_b = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _b["author"]) || "";
    const emptyOption = select.createEl("option");
    emptyOption.text = "\u041D\u0435 \u043D\u0430\u0437\u043D\u0430\u0447\u0435\u043D";
    emptyOption.value = "";
    if (!currentValue)
      emptyOption.selected = true;
    allMembers.forEach((member) => {
      const option = select.createEl("option");
      option.text = member;
      option.value = member;
      if (currentValue === member)
        option.selected = true;
    });
    if (currentValue && !allMembers.includes(currentValue)) {
      const customOption = select.createEl("option");
      customOption.text = currentValue + " (\u0442\u0435\u043A\u0443\u0449\u0438\u0439)";
      customOption.value = currentValue;
      customOption.selected = true;
    }
    const customContainer = contentEl.createDiv();
    customContainer.style.marginBottom = "20px";
    const span = customContainer.createEl("span");
    span.textContent = "\u0418\u043B\u0438 \u0432\u0432\u0435\u0434\u0438\u0442\u0435 \u043D\u043E\u0432\u043E\u0435 \u0438\u043C\u044F:";
    span.style.display = "block";
    span.style.marginBottom = "8px";
    const input = new import_obsidian15.TextComponent(customContainer);
    input.setPlaceholder("\u041D\u043E\u0432\u043E\u0435 \u0438\u043C\u044F");
    input.inputEl.style.width = "100%";
    new import_obsidian15.ButtonComponent(contentEl).setButtonText("+ \u0414\u043E\u0431\u0430\u0432\u0438\u0442\u044C \u0432 \u043A\u043E\u043C\u0430\u043D\u0434\u0443").setClass("mod-small").onClick(async () => {
      const newName = input.inputEl.value.trim();
      if (newName) {
        await this.addToTeam(newName);
        this.close();
      }
    });
    const btnContainer = contentEl.createDiv();
    btnContainer.style.display = "flex";
    btnContainer.style.gap = "10px";
    btnContainer.style.marginTop = "16px";
    new import_obsidian15.ButtonComponent(btnContainer).setButtonText("\u0421\u043E\u0445\u0440\u0430\u043D\u0438\u0442\u044C").setCta().onClick(() => {
      const value = input.inputEl.value.trim() || select.value;
      this.onSave(value);
      this.close();
    });
    new import_obsidian15.ButtonComponent(btnContainer).setButtonText("\u041E\u0442\u043C\u0435\u043D\u0430").onClick(() => this.close());
  }
  async addToTeam(name) {
    const routinesFile = this.app.vault.getAbstractFileByPath("routines.md");
    if (!routinesFile || !(routinesFile instanceof import_obsidian15.TFile)) {
      const content = `# \u041A\u043E\u043C\u0430\u043D\u0434\u0430
- ${name}
`;
      await this.app.vault.create("routines.md", content);
    } else {
      const content = await this.app.vault.read(routinesFile);
      if (!content.includes("# \u041A\u043E\u043C\u0430\u043D\u0434\u0430") && !content.includes("# \u043A\u043E\u043C\u0430\u043D\u0434\u0430")) {
        const newContent = content + "\n# \u041A\u043E\u043C\u0430\u043D\u0434\u0430\n- " + name + "\n";
        await this.app.vault.modify(routinesFile, newContent);
      } else if (!content.includes("- " + name)) {
        const lines = content.split("\n");
        let inTeamSection = false;
        const newLines = [];
        for (const line of lines) {
          if (line.trim().toLowerCase() === "# \u043A\u043E\u043C\u0430\u043D\u0434\u0430") {
            inTeamSection = true;
          }
          if (inTeamSection && (line.startsWith("# ") || line.startsWith("## "))) {
            newLines.push(line);
            newLines.push("- " + name);
            inTeamSection = false;
            continue;
          }
          newLines.push(line);
        }
        if (inTeamSection) {
          newLines.push("- " + name);
        }
        await this.app.vault.modify(routinesFile, newLines.join("\n"));
      }
    }
    this.onSave(name);
  }
  get settings() {
    var _a;
    return ((_a = this.app.plugins.plugins["monitoring-plugin"]) == null ? void 0 : _a.settings) || DEFAULT_SETTINGS;
  }
};

// src/modals/EmailTopicModal.ts
var import_obsidian16 = require("obsidian");
var EmailTopicModal = class extends import_obsidian16.Modal {
  constructor(app, file, onSave) {
    super(app);
    this.topics = [];
    this.newTopic = "";
    this.file = file;
    this.onSave = onSave;
  }
  async onOpen() {
    var _a;
    const { contentEl } = this;
    const cache = this.app.metadataCache.getFileCache(this.file);
    this.topics = ((_a = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _a["tracked_emails"]) || [];
    this.render(contentEl);
  }
  render(contentEl) {
    contentEl.empty();
    contentEl.createEl("h3", { text: "\u041E\u0442\u0441\u043B\u0435\u0436\u0438\u0432\u0430\u043D\u0438\u0435 \u043F\u0438\u0441\u0435\u043C" });
    const desc = contentEl.createEl("p");
    desc.textContent = "\u0414\u043E\u0431\u0430\u0432\u044C\u0442\u0435 \u0442\u0435\u043C\u044B \u043F\u0438\u0441\u0435\u043C \u0434\u043B\u044F \u043E\u0442\u0441\u043B\u0435\u0436\u0438\u0432\u0430\u043D\u0438\u044F. \u0412\u0441\u0435 \u043F\u0438\u0441\u044C\u043C\u0430 \u0441 \u044D\u0442\u0438\u043C\u0438 \u0442\u0435\u043C\u0430\u043C\u0438 \u0431\u0443\u0434\u0443\u0442 \u0434\u043E\u0431\u0430\u0432\u043B\u044F\u0442\u044C\u0441\u044F \u0432 \u043F\u0440\u043E\u0435\u043A\u0442.";
    const listContainer = contentEl.createDiv({ cls: "email-topics-list" });
    this.renderTopicsList(listContainer);
    const addSection = contentEl.createDiv({ cls: "email-topics-add" });
    addSection.style.marginTop = "16px";
    const input = new import_obsidian16.TextComponent(addSection);
    input.setPlaceholder("\u041D\u043E\u0432\u0430\u044F \u0442\u0435\u043C\u0430 \u043F\u0438\u0441\u044C\u043C\u0430");
    input.inputEl.style.width = "70%";
    input.onChange((v) => this.newTopic = v);
    new import_obsidian16.ButtonComponent(addSection).setButtonText("+").onClick(() => {
      if (this.newTopic.trim() && !this.topics.includes(this.newTopic.trim())) {
        this.topics.push(this.newTopic.trim());
        this.newTopic = "";
        input.setValue("");
        this.renderTopicsList(contentEl.querySelector(".email-topics-list"));
      }
    });
    const btnContainer = contentEl.createDiv();
    btnContainer.style.display = "flex";
    btnContainer.style.gap = "10px";
    btnContainer.style.marginTop = "20px";
    new import_obsidian16.ButtonComponent(btnContainer).setButtonText("\u0421\u043E\u0445\u0440\u0430\u043D\u0438\u0442\u044C").setCta().onClick(() => {
      this.onSave(this.topics);
      this.close();
    });
    new import_obsidian16.ButtonComponent(btnContainer).setButtonText("\u041E\u0442\u043C\u0435\u043D\u0430").onClick(() => this.close());
  }
  renderTopicsList(container) {
    container.empty();
    if (this.topics.length === 0) {
      container.createEl("p", { text: "\u0422\u0435\u043C\u044B \u043F\u043E\u043A\u0430 \u043D\u0435 \u0434\u043E\u0431\u0430\u0432\u043B\u0435\u043D\u044B", cls: "empty-text" });
      return;
    }
    this.topics.forEach((topic, idx) => {
      const item = container.createDiv({ cls: "email-topic-item" });
      item.createSpan({ text: topic, cls: "email-topic-text" });
      const deleteBtn = item.createEl("button", {
        text: "\xD7",
        cls: "email-topic-delete",
        attr: { title: "\u0423\u0434\u0430\u043B\u0438\u0442\u044C" }
      });
      deleteBtn.onclick = () => {
        this.topics.splice(idx, 1);
        this.renderTopicsList(container);
      };
    });
  }
};

// src/MonitoringDurationChild.ts
var MonitoringDurationChild = class extends import_obsidian17.MarkdownRenderChild {
  constructor(containerEl, plugin, file) {
    super(containerEl);
    this.plugin = plugin;
    this.file = file;
  }
  async onload() {
    this.rootContainer = this.containerEl.createDiv({ cls: "monitoring-duration-wrapper" });
    await this.renderUI();
    this.registerEvent(this.plugin.app.metadataCache.on("changed", (f) => {
      if (f.path === this.file.path)
        this.renderUI();
    }));
  }
  async renderUI() {
    var _a, _b;
    const rootContainer = this.rootContainer;
    rootContainer.empty();
    const cache = this.plugin.app.metadataCache.getFileCache(this.file);
    let currentDeadline = ((_a = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _a["deadline"]) || "";
    const isSimpleNote = ((_b = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _b["daily"]) === true;
    const renderCollapsed = () => {
      var _a2, _b2, _c, _d, _e;
      rootContainer.empty();
      const panelContainer = rootContainer.createDiv({ cls: "monitoring-controls-panel" });
      if (!isSimpleNote) {
        const row1 = panelContainer.createDiv({ cls: "monitoring-split-row" });
        const dContainer = row1.createDiv({ cls: "monitoring-third-row" });
        dContainer.createEl("button", {
          cls: "monitoring-glass-btn monitoring-btn-full",
          text: currentDeadline ? `\u23F3 ${currentDeadline}` : "\u{1F4C5} \u0421\u0440\u043E\u043A"
        }).onclick = () => renderExpanded();
        const respContainer = row1.createDiv({ cls: "monitoring-third-row" });
        const currentResponsible = ((_a2 = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _a2["responsible"]) || this.plugin.settings.currentUser || "";
        const respBtn = respContainer.createEl("button", {
          cls: "monitoring-glass-btn monitoring-btn-full",
          text: currentResponsible ? `\u{1F464} ${currentResponsible}` : "\u{1F464} \u041E\u0442\u0432\u0435\u0442\u0441\u0442\u0432\u0435\u043D\u043D\u044B\u0439"
        });
        respBtn.onclick = () => new ResponsibleButtonModal(this.plugin.app, this.file, async (name) => {
          await this.plugin.app.fileManager.processFrontMatter(this.file, (fm) => {
            fm["responsible"] = name;
          });
        }).open();
        const emailContainer = row1.createDiv({ cls: "monitoring-third-row" });
        const trackedEmails = ((_b2 = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _b2["tracked_emails"]) || [];
        const emailCount = Array.isArray(trackedEmails) ? trackedEmails.length : 0;
        const emailBtn = emailContainer.createEl("button", {
          cls: "monitoring-glass-btn monitoring-btn-full",
          text: emailCount > 0 ? `\u{1F4E7} ${emailCount} \u0442\u0435\u043C` : "\u{1F4E7} \u041F\u043E\u0447\u0442\u0430"
        });
        emailBtn.onclick = () => new EmailTopicModal(this.plugin.app, this.file, async (topics) => {
          await this.plugin.app.fileManager.processFrontMatter(this.file, (fm) => {
            fm["tracked_emails"] = topics;
          });
          this.rootContainer.empty();
          await this.renderUI();
        }).open();
        const pContainer = row1.createDiv({ cls: "monitoring-third-row" });
        let isPExp = false;
        const renderPUI = () => {
          var _a3;
          pContainer.empty();
          const priority = parseInt((_a3 = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _a3["priority"]) || 3;
          if (!isPExp) {
            const tBtn = pContainer.createEl("button", { cls: "monitoring-glass-btn priority-toggle-btn monitoring-btn-full" });
            tBtn.createSpan({ text: "\u2B50 ", attr: { style: "margin-right: 4px;" } });
            tBtn.createSpan({ text: priority.toString(), cls: `priority-badge priority-${priority}` });
            tBtn.onclick = () => {
              isPExp = true;
              renderPUI();
            };
          } else {
            const wrap = pContainer.createDiv({ cls: "priority-slider-mini-wrapper" });
            const slider = wrap.createEl("input", { type: "range", cls: "priority-slider", attr: { min: "1", max: "5", value: priority.toString() } });
            slider.onchange = async () => {
              await this.plugin.app.fileManager.processFrontMatter(this.file, (fm) => {
                fm["priority"] = parseInt(slider.value);
              });
              isPExp = false;
              renderPUI();
            };
            wrap.createEl("button", { cls: "mini-close-btn", text: "\xD7" }).onclick = () => {
              isPExp = false;
              renderPUI();
            };
          }
        };
        renderPUI();
        const statusRow = panelContainer.createDiv({ cls: "monitoring-status-row segmented-control" });
        [{ l: "\u041F\u043B\u0430\u043D", v: "To Do", i: "\u{1F3AF}" }, { l: "\u0412 \u0440\u0430\u0431\u043E\u0442\u0435", v: "In Progress", i: "\u26A1" }, { l: "\u0413\u043E\u0442\u043E\u0432\u043E", v: "Done", i: "\u2705" }].forEach((s) => {
          var _a3;
          const b = statusRow.createEl("button", { cls: "monitoring-glass-btn status-segment-btn", text: `${s.i} ${s.l}` });
          if (((_a3 = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _a3["status"]) === s.v)
            b.addClass("is-active-status");
          b.onclick = async () => {
            await this.plugin.app.fileManager.processFrontMatter(this.file, (fm) => {
              fm["status"] = s.v;
            });
            if (this.file.basename.startsWith("Task-")) {
              await this.plugin.templateManager.updateSubtaskStatusIcon(this.file);
            }
          };
        });
      } else {
        const row1 = panelContainer.createDiv({ cls: "monitoring-split-row" });
        const dateContainer = row1.createDiv({ cls: "monitoring-half-row" });
        const createdDate = ((_c = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _c["created"]) || "";
        dateContainer.createEl("button", {
          cls: "monitoring-glass-btn monitoring-btn-full",
          text: createdDate ? `\u{1F4C5} ${createdDate}` : "\u{1F4C5} \u0414\u0430\u0442\u0430"
        }).onclick = () => {
        };
        const authorContainer = row1.createDiv({ cls: "monitoring-half-row" });
        const currentAuthor = ((_d = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _d["author"]) || this.plugin.settings.currentUser || "";
        const authorBtn = authorContainer.createEl("button", {
          cls: "monitoring-glass-btn monitoring-btn-full",
          text: currentAuthor ? `\u270D\uFE0F ${currentAuthor}` : "\u270D\uFE0F \u0410\u0432\u0442\u043E\u0440"
        });
        authorBtn.onclick = () => new ResponsibleButtonModal(this.plugin.app, this.file, async (name) => {
          await this.plugin.app.fileManager.processFrontMatter(this.file, (fm) => {
            fm["author"] = name;
          });
        }).open();
      }
      const toolsRow = panelContainer.createDiv({ cls: "monitoring-tools-row" });
      toolsRow.createEl("button", { cls: "monitoring-glass-btn tool-btn", text: "\u2795 \u0417\u0430\u0434\u0430\u0447\u0443" }).onclick = () => {
        const projectName = this.file.basename.replace(/^Project-/, "");
        const tag = "Project" + projectName.replace(/\s+/g, "").replace(/[^\w\u0400-\u04FF]/g, "");
        new NewTaskModal(this.plugin.app, async (name) => {
          const newF = await this.plugin.templateManager.createTaskNote(name, [tag], projectName);
          if (newF) {
            await this.plugin.templateManager.updateSubtaskTable(this.file, newF);
            await this.plugin.app.workspace.getLeaf(false).openFile(newF);
          }
        }).open();
      };
      toolsRow.createEl("button", { cls: "monitoring-glass-btn tool-btn", text: "\u{1F517} \u0420\u0435\u0441\u0443\u0440\u0441" }).onclick = () => new ResourceModal(this.plugin.app, (l, d) => this.plugin.addResourceToNote(this.file, l, d)).open();
      toolsRow.createEl("button", { cls: "monitoring-glass-btn tool-btn", text: "\u{1F3F7}\uFE0F \u0422\u0435\u0433" }).onclick = () => new TagModal(this.plugin.app, (t) => this.plugin.addTagToNote(this.file, t)).open();
      const footer = panelContainer.createDiv({ cls: "monitoring-footer-row" });
      footer.createEl("button", { cls: "monitoring-glass-btn footer-btn", text: "\u{1F4E6} \u0410\u0440\u0445\u0438\u0432" }).onclick = () => this.plugin.moveFileToFolder(this.file, "\u0410\u0440\u0445\u0438\u0432");
      footer.createEl("button", { cls: "monitoring-glass-btn footer-btn", text: "\u{1F5D1}\uFE0F \u041A\u043E\u0440\u0437\u0438\u043D\u0430" }).onclick = () => this.plugin.moveFileToFolder(this.file, "\u041A\u043E\u0440\u0437\u0438\u043D\u0430");
      footer.createEl("button", { cls: "monitoring-glass-btn footer-btn delete-btn", text: "\u274C \u0423\u0434\u0430\u043B\u0438\u0442\u044C" }).onclick = async () => {
        if (confirm(`\u0423\u0434\u0430\u043B\u0438\u0442\u044C "${this.file.basename}"?`))
          await this.plugin.app.vault.delete(this.file);
      };
      const tagList = rootContainer.createDiv({ cls: "monitoring-tag-list" });
      const tags = ((_e = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _e["tags"]) || [];
      (Array.isArray(tags) ? tags : [tags]).forEach((tag) => {
        const pill = tagList.createSpan({ cls: "monitoring-tag-pill", text: `#${tag}` });
        pill.onclick = () => {
          if (confirm(`\u0423\u0434\u0430\u043B\u0438\u0442\u044C #${tag}?`))
            this.plugin.removeTagFromNote(this.file, tag);
        };
      });
    };
    const renderExpanded = () => {
      rootContainer.empty();
      const container = rootContainer.createDiv({ cls: "monitoring-duration-ui" });
      let viewedMonth = new Date();
      viewedMonth.setDate(1);
      let startDate = null;
      let endDate = null;
      if (currentDeadline) {
        try {
          const parts = currentDeadline.split(" to ");
          if (parts.length === 2) {
            startDate = new Date(parts[0]);
            endDate = new Date(parts[1].split(" ")[0]);
          } else {
            startDate = new Date(currentDeadline.split(" ")[0]);
          }
        } catch (e) {
        }
      }
      const calendarBody = container.createDiv();
      const updateCalendar = () => {
        calendarBody.empty();
        const header = calendarBody.createDiv({ cls: "monitoring-calendar-header" });
        header.createEl("button", { cls: "calendar-nav-btn", text: "\u2039" }).onclick = () => {
          viewedMonth.setMonth(viewedMonth.getMonth() - 1);
          updateCalendar();
        };
        header.createSpan({ cls: "calendar-month-label", text: `${["\u042F\u043D\u0432\u0430\u0440\u044C", "\u0424\u0435\u0432\u0440\u0430\u043B\u044C", "\u041C\u0430\u0440\u0442", "\u0410\u043F\u0440\u0435\u043B\u044C", "\u041C\u0430\u0439", "\u0418\u044E\u043D\u044C", "\u0418\u044E\u043B\u044C", "\u0410\u0432\u0433\u0443\u0441\u0442", "\u0421\u0435\u043D\u0442\u044F\u0431\u0440\u044C", "\u041E\u043A\u0442\u044F\u0431\u0440\u044C", "\u041D\u043E\u044F\u0431\u0440\u044C", "\u0414\u0435\u043A\u0430\u0431\u0440\u044C"][viewedMonth.getMonth()]} ${viewedMonth.getFullYear()}` });
        header.createEl("button", { cls: "calendar-nav-btn", text: "\u203A" }).onclick = () => {
          viewedMonth.setMonth(viewedMonth.getMonth() + 1);
          updateCalendar();
        };
        const weekGrid = calendarBody.createDiv({ cls: "calendar-weekday-labels" });
        ["\u041F\u043D", "\u0412\u0442", "\u0421\u0440", "\u0427\u0442", "\u041F\u0442", "\u0421\u0431", "\u0412\u0441"].forEach((d) => weekGrid.createSpan({ text: d }));
        const daysGrid = calendarBody.createDiv({ cls: "calendar-days-grid" });
        const firstDay = new Date(viewedMonth.getFullYear(), viewedMonth.getMonth(), 1).getDay() || 7;
        const daysInMonth = new Date(viewedMonth.getFullYear(), viewedMonth.getMonth() + 1, 0).getDate();
        const todayStr = new Date().toISOString().split("T")[0];
        for (let i = 1; i < firstDay; i++)
          daysGrid.createSpan({ cls: "calendar-empty-cell" });
        let counter = new Date(viewedMonth.getFullYear(), viewedMonth.getMonth(), 1);
        for (let d = 1; d <= daysInMonth; d++) {
          counter = new Date(viewedMonth.getFullYear(), viewedMonth.getMonth(), d);
          const dStr = counter.toISOString().split("T")[0];
          const cell = daysGrid.createDiv({ cls: "calendar-day-cell" });
          cell.createSpan({ cls: "calendar-day-num", text: d.toString() });
          if (d.getMonth() !== viewedMonth.getMonth())
            cell.addClass("is-other-month");
          if (dStr === todayStr)
            cell.createDiv({ cls: "calendar-today-mark" });
          if (startDate && dStr === startDate.toISOString().split("T")[0] || endDate && dStr === endDate.toISOString().split("T")[0])
            cell.addClass("is-selected");
          if (startDate && endDate && d > startDate && d < endDate)
            cell.addClass("is-in-range");
          cell.onclick = () => {
            if (!startDate || startDate && endDate) {
              startDate = d;
              endDate = null;
            } else if (startDate) {
              if (d > startDate)
                endDate = d;
              else {
                startDate = d;
                endDate = null;
              }
            }
            updateCalendar();
          };
          counter.setDate(counter.getDate() + 1);
        }
        syncInputs();
      };
      const controls = container.createDiv({ cls: "duration-controls", attr: { style: "flex-direction: column; align-items: stretch; gap: 10px; display: flex;" } });
      const inputsRow = controls.createDiv({ attr: { style: "display: flex; gap: 10px; align-items: center; flex-wrap: wrap;" } });
      const sInput = inputsRow.createEl("input", { type: "date", cls: "duration-date-input" });
      const toSpan = inputsRow.createSpan({ text: "\u0434\u043E", attr: { style: "opacity: 0.6;" } });
      const eInput = inputsRow.createEl("input", { type: "date", cls: "duration-date-input" });
      const tPicker = inputsRow.createDiv({ cls: "duration-time-picker" });
      tPicker.createSpan({ text: "\u23F0", attr: { style: "margin-right: 5px;" } });
      const tInput = tPicker.createEl("input", { type: "time" });
      tInput.value = currentDeadline.includes(":") ? currentDeadline.split(" ").pop() || "10:00" : "10:00";
      const syncInputs = () => {
        if (startDate)
          sInput.value = startDate.toISOString().split("T")[0];
        if (endDate) {
          eInput.value = endDate.toISOString().split("T")[0];
          eInput.style.display = "block";
          toSpan.style.display = "inline";
        } else {
          eInput.style.display = "none";
          toSpan.style.display = "none";
        }
      };
      const bRow = controls.createDiv({ attr: { style: "display: flex; gap: 10px; justify-content: flex-end;" } });
      bRow.createEl("button", { text: "\u0421\u043E\u0445\u0440\u0430\u043D\u0438\u0442\u044C", cls: "monitoring-report-btn" }).onclick = async () => {
        let dStr = sInput.value;
        if (eInput.style.display !== "none" && eInput.value)
          dStr += ` to ${eInput.value}`;
        dStr += ` ${tInput.value}`;
        await this.plugin.app.fileManager.processFrontMatter(this.file, (fm) => {
          fm["deadline"] = dStr;
        });
        renderCollapsed();
      };
      bRow.createEl("button", { text: "\u041E\u0442\u043C\u0435\u043D\u0430", cls: "monitoring-glass-btn" }).onclick = () => renderCollapsed();
      updateCalendar();
    };
    renderCollapsed();
  }
};

// src/main.ts
var MonitoringPlugin = class extends import_obsidian18.Plugin {
  async onload() {
    await this.loadSettings();
    this.llmService = new LLMService(this.settings);
    this.outlookService = new OutlookService(this.settings, this.app);
    this.templateManager = new TemplateManager(this.app, this.settings);
    this.dailyService = new DailyService(this.app, this.settings);
    this.registerView(
      CHAT_VIEW_TYPE,
      (leaf) => new ChatView(leaf, this.llmService)
    );
    this.registerView(
      MAIN_PAGE_VIEW_TYPE,
      (leaf) => new MainPageView(leaf, this)
    );
    this.addRibbonIcon("mail", "\u0418\u043C\u043F\u043E\u0440\u0442\u0438\u0440\u043E\u0432\u0430\u0442\u044C \u043F\u043E\u0447\u0442\u0443", async () => {
      await this.processEmails();
    });
    this.addRibbonIcon("message-square", "\u041A\u043E\u0440\u043F\u043E\u0440\u0430\u0442\u0438\u0432\u043D\u044B\u0439 \u0418\u0418 \u0427\u0430\u0442", async () => {
      await this.activateChatView();
    });
    this.addRibbonIcon("brain", "\u0413\u043B\u0430\u0432\u043D\u0430\u044F \u043F\u0430\u043D\u0435\u043B\u044C \u043F\u0440\u043E\u0435\u043A\u0442\u043E\u0432", async () => {
      await this.activateMainPageView();
    });
    this.addCommand({
      id: "import-mail",
      name: "\u041E\u0431\u043D\u043E\u0432\u0438\u0442\u044C \u043F\u043E\u0447\u0442\u0443 (Import Mail)",
      callback: async () => {
        await this.processEmails();
      }
    });
    this.addCommand({
      id: "open-ai-chat",
      name: "\u041E\u0442\u043A\u0440\u044B\u0442\u044C \u043A\u043E\u0440\u043F\u043E\u0440\u0430\u0442\u0438\u0432\u043D\u044B\u0439 \u0447\u0430\u0442 (Open AI Chat)",
      callback: async () => {
        await this.activateChatView();
      }
    });
    this.registerMarkdownCodeBlockProcessor("monitoring-ui", (source, el, ctx) => {
      const container = el.createDiv({ cls: "monitoring-dashboard-ui" });
      container.createEl("h4", { text: "\u0423\u043F\u0440\u0430\u0432\u043B\u0435\u043D\u0438\u0435 \u043E\u0442\u0441\u043B\u0435\u0436\u0438\u0432\u0430\u043D\u0438\u0435\u043C" });
      const wrapper = container.createDiv({ cls: "monitoring-input-wrapper" });
      const input = wrapper.createEl("input", {
        type: "text",
        placeholder: "\u0412\u0432\u0435\u0434\u0438\u0442\u0435 \u043A\u043B\u044E\u0447\u0435\u0432\u044B\u0435 \u0441\u043B\u043E\u0432\u0430 \u0442\u0435\u043C\u044B...",
        cls: "monitoring-topic-input"
      });
      const btn = wrapper.createEl("button", { text: "\u0414\u043E\u0431\u0430\u0432\u0438\u0442\u044C", cls: "monitoring-add-btn" });
      btn.onclick = async () => {
        const topic = input.value.trim();
        if (!topic)
          return;
        const dashboardFile = this.app.vault.getAbstractFileByPath(this.settings.dashboardNoteName);
        if (dashboardFile) {
          await this.app.fileManager.processFrontMatter(dashboardFile, (fm) => {
            if (!fm["tracked_subjects"])
              fm["tracked_subjects"] = [];
            if (!Array.isArray(fm["tracked_subjects"]))
              fm["tracked_subjects"] = [fm["tracked_subjects"]];
            if (!fm["tracked_subjects"].includes(topic))
              fm["tracked_subjects"].push(topic);
          });
          new import_obsidian18.Notice(`\u0422\u0435\u043C\u0430 "${topic}" \u0434\u043E\u0431\u0430\u0432\u043B\u0435\u043D\u0430!`);
          input.value = "";
        } else {
          new import_obsidian18.Notice("\u0424\u0430\u0439\u043B \u0434\u0430\u0448\u0431\u043E\u0440\u0434\u0430 \u043D\u0435 \u043D\u0430\u0439\u0434\u0435\u043D!");
        }
      };
    });
    this.registerMarkdownCodeBlockProcessor("monitoring-duration", (source, el, ctx) => {
      const file = this.app.vault.getAbstractFileByPath(ctx.sourcePath);
      if (!(file instanceof import_obsidian18.TFile))
        return;
      const child = new MonitoringDurationChild(el, this, file);
      ctx.addChild(child);
    });
    this.registerMarkdownPostProcessor((el) => {
      const activeFile = this.app.workspace.getActiveFile();
      if (!activeFile || !(activeFile instanceof import_obsidian18.TFile))
        return;
      if (!activeFile.path.match(/^daily-.*\//))
        return;
      if (el.querySelector(".monitoring-duration-container"))
        return;
      const container = el.createDiv({ cls: "monitoring-duration-container" });
      container.style.marginBottom = "20px";
      const child = new MonitoringDurationChild(container, this, activeFile);
      child.onload();
    });
    this.addSettingTab(new MonitoringSettingTab(this.app, this));
    this.app.workspace.on("editor-menu", (menu, editor, info) => {
      const selection = editor.getSelection();
      if (!selection)
        return;
      menu.addItem((item) => {
        item.setTitle("\u0418\u0418: \u041E\u0440\u0444\u043E\u0433\u0440\u0430\u0444\u0438\u044F").setIcon("check").onClick(async () => {
          new import_obsidian18.Notice("\u041F\u0440\u043E\u0432\u0435\u0440\u043A\u0430 \u043E\u0440\u0444\u043E\u0433\u0440\u0430\u0444\u0438\u0438...");
          try {
            const fixed = await this.llmService.checkSpelling(selection);
            editor.replaceSelection(fixed);
            new import_obsidian18.Notice("\u041E\u0440\u0444\u043E\u0433\u0440\u0430\u0444\u0438\u044F \u0438\u0441\u043F\u0440\u0430\u0432\u043B\u0435\u043D\u0430");
          } catch (e) {
            new import_obsidian18.Notice("\u041E\u0448\u0438\u0431\u043A\u0430: " + e.message);
          }
        });
      });
      menu.addItem((item) => {
        item.setTitle("\u0418\u0418: \u041F\u0435\u0440\u0435\u0444\u043E\u0440\u043C\u0443\u043B\u0438\u0440\u043E\u0432\u0430\u0442\u044C").setIcon("refresh-cw").onClick(async () => {
          new import_obsidian18.Notice("\u041F\u0435\u0440\u0435\u0444\u043E\u0440\u043C\u0443\u043B\u0438\u0440\u043E\u0432\u0430\u043D\u0438\u0435...");
          try {
            const rephrased = await this.llmService.rephrase(selection);
            editor.replaceSelection(rephrased);
            new import_obsidian18.Notice("\u0422\u0435\u043A\u0441\u0442 \u043F\u0435\u0440\u0435\u0444\u043E\u0440\u043C\u0443\u043B\u0438\u0440\u043E\u0432\u0430\u043D");
          } catch (e) {
            new import_obsidian18.Notice("\u041E\u0448\u0438\u0431\u043A\u0430: " + e.message);
          }
        });
      });
    });
  }
  async activateChatView() {
    const { workspace } = this.app;
    let leaf = workspace.getLeavesOfType(CHAT_VIEW_TYPE)[0];
    if (!leaf) {
      leaf = workspace.getRightLeaf(false);
      if (leaf)
        await leaf.setViewState({ type: CHAT_VIEW_TYPE, active: true });
    }
    if (leaf)
      workspace.revealLeaf(leaf);
  }
  async activateMainPageView() {
    const { workspace } = this.app;
    let leaf = workspace.getLeavesOfType(MAIN_PAGE_VIEW_TYPE)[0];
    if (!leaf) {
      leaf = workspace.getLeaf(true);
      await leaf.setViewState({ type: MAIN_PAGE_VIEW_TYPE, active: true });
    }
    if (leaf)
      workspace.revealLeaf(leaf);
  }
  async processEmails() {
    var _a, _b;
    new import_obsidian18.Notice("\u0417\u0430\u043F\u0443\u0441\u043A \u0441\u043A\u0430\u043D\u0438\u0440\u043E\u0432\u0430\u043D\u0438\u044F \u043F\u043E\u0447\u0442\u044B...");
    try {
      const allEmails = await this.outlookService.fetchEmails();
      allEmails.reverse();
      let newEmails = allEmails.filter((e) => !this.settings.scannedEmailIds.includes(e.entryId));
      if (newEmails.length === 0)
        newEmails = allEmails.slice(Math.max(allEmails.length - 10, 0));
      if (newEmails.length === 0) {
        new import_obsidian18.Notice("\u041F\u0430\u043F\u043A\u0430 \u043F\u0443\u0441\u0442\u0430.");
        return;
      }
      const files = this.app.vault.getMarkdownFiles();
      const projectFiles = [];
      for (const file of files) {
        const cache = this.app.metadataCache.getFileCache(file);
        const type = (_a = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _a["type"];
        if (type === "project") {
          const tracked = ((_b = cache == null ? void 0 : cache.frontmatter) == null ? void 0 : _b["tracked_emails"]) || [];
          if (tracked.length > 0) {
            projectFiles.push({
              file,
              tracked_emails: tracked.map((t) => t.toLowerCase())
            });
          }
        }
      }
      let processedCount = 0;
      for (const email of newEmails) {
        const topicLower = email.conversationTopic.toLowerCase();
        const matchedProjects = projectFiles.filter(
          (p) => p.tracked_emails.some((te) => topicLower.includes(te))
        );
        if (matchedProjects.length > 0) {
          for (const project of matchedProjects) {
            await this.addEmailToProject(project.file, email);
          }
        } else {
          const existingNote = await this.templateManager.getIncidentNoteByTopic(email.conversationTopic);
          if (existingNote) {
            const content = await this.templateManager.readNoteContent(existingNote);
            const match = content.match(/## Текущее саммари инцидента\n([\s\S]*?)\n---/m);
            const oldSummary = match ? match[1].trim() : "\u041B\u043E\u0433\u043E\u0432 \u043D\u0435\u0442.";
            const combinedSummary = await this.llmService.updateIncidentSummary(oldSummary, email.bodyPreview);
            await this.templateManager.updateIncidentNote(existingNote, email, combinedSummary);
          } else {
            const summary = await this.llmService.summarizeIncident(email.bodyPreview);
            await this.templateManager.createIncidentNote(email, summary);
          }
        }
        processedCount++;
        if (!this.settings.scannedEmailIds.includes(email.entryId))
          this.settings.scannedEmailIds.push(email.entryId);
      }
      await this.saveSettings();
      new import_obsidian18.Notice(processedCount === 0 ? "\u041D\u0435\u0442 \u043F\u0438\u0441\u0435\u043C \u043F\u043E \u043E\u0442\u0441\u043B\u0435\u0436\u0438\u0432\u0430\u0435\u043C\u044B\u043C \u0442\u0435\u043C\u0430\u043C." : `\u0413\u043E\u0442\u043E\u0432\u043E! \u041E\u0431\u0440\u0430\u0431\u043E\u0442\u0430\u043D\u043E \u043F\u0438\u0441\u0435\u043C: ${processedCount}`);
    } catch (error) {
      console.error(error);
      new import_obsidian18.Notice("\u041E\u0448\u0438\u0431\u043A\u0430 \u043F\u0440\u0438 \u0438\u043C\u043F\u043E\u0440\u0442\u0435: " + error.message);
    }
  }
  async addEmailToProject(projectFile, email) {
    const vault = this.app.vault;
    const content = await vault.read(projectFile);
    const emailEntry = `- [${email.conversationTopic}](${email.subject}) \u2014 ${email.sender} (${new Date(email.receivedDateTime).toLocaleDateString("ru-RU")})`;
    let newContent = content;
    if (content.includes("## \u{1F4EC} \u041F\u0438\u0441\u044C\u043C\u0430")) {
      const emailSectionMatch = content.match(/(## 📬 Письма\n)([\s\S]*)/);
      if (emailSectionMatch) {
        newContent = emailSectionMatch[1] + emailEntry + "\n" + emailSectionMatch[2];
      }
    } else {
      newContent = content + "\n## \u{1F4EC} \u041F\u0438\u0441\u044C\u043C\u0430\n" + emailEntry + "\n";
    }
    await vault.modify(projectFile, newContent);
  }
  onunload() {
  }
  async loadSettings() {
    this.settings = Object.assign({}, DEFAULT_SETTINGS, await this.loadData());
  }
  async saveSettings() {
    await this.saveData(this.settings);
    this.llmService.updateSettings(this.settings);
    this.outlookService.updateSettings(this.settings);
    this.templateManager.updateSettings(this.settings);
  }
  async addResourceToNote(file, link, description) {
    const content = await this.app.vault.read(file);
    const header = "## \u{1F517} \u0411\u0430\u0437\u0430 \u043C\u0430\u0442\u0435\u0440\u0438\u0430\u043B\u043E\u0432 \u0438 \u0440\u0435\u0441\u0443\u0440\u0441\u043E\u0432";
    const normalizedLink = link.startsWith("http") ? `[\u041E\u0442\u043A\u0440\u044B\u0442\u044C](${link})` : `[\u041E\u0442\u043A\u0440\u044B\u0442\u044C](file:///${link.replace(/\\/g, "/")})`;
    const newRow = `| ${normalizedLink} | ${description || link} |
`;
    let newContent = content.includes(header) ? content.replace(header, `${header}
${newRow}`) : content + `

${header}
| \u0420\u0435\u0441\u0443\u0440\u0441 | \u041E\u043F\u0438\u0441\u0430\u043D\u0438\u0435 |
| --- | --- |
${newRow}`;
    await this.app.vault.modify(file, newContent);
  }
  async addTagToNote(file, tag) {
    await this.app.fileManager.processFrontMatter(file, (fm) => {
      let tags = fm["tags"] || [];
      if (typeof tags === "string")
        tags = tags.split(",").map((t) => t.trim());
      if (!Array.isArray(tags))
        tags = [tags];
      const cleanTag = tag.startsWith("#") ? tag.substring(1) : tag;
      if (!tags.includes(cleanTag)) {
        tags.push(cleanTag);
        fm["tags"] = tags;
      }
    });
  }
  async removeTagFromNote(file, tag) {
    await this.app.fileManager.processFrontMatter(file, (fm) => {
      let tags = fm["tags"] || [];
      if (typeof tags === "string")
        tags = tags.split(",").map((t) => t.trim());
      if (!Array.isArray(tags))
        tags = [tags];
      const cleanTag = tag.startsWith("#") ? tag.substring(1) : tag;
      fm["tags"] = tags.filter((t) => t !== cleanTag);
    });
  }
  async moveFileToFolder(file, folderName) {
    try {
      if (!this.app.vault.getAbstractFileByPath(folderName))
        await this.app.vault.createFolder(folderName);
      const newPath = `${folderName}/${file.name}`;
      if (this.app.vault.getAbstractFileByPath(newPath)) {
        new import_obsidian18.Notice(`\u0424\u0430\u0439\u043B \u0443\u0436\u0435 \u0441\u0443\u0449\u0435\u0441\u0442\u0432\u0443\u0435\u0442 \u0432 "${folderName}"`);
        return;
      }
      await this.app.fileManager.renameFile(file, newPath);
      new import_obsidian18.Notice(`\u0417\u0430\u043C\u0435\u0442\u043A\u0430 \u043F\u0435\u0440\u0435\u043C\u0435\u0449\u0435\u043D\u0430 \u0432 "${folderName}"`);
    } catch (e) {
      console.error(e);
      new import_obsidian18.Notice(`\u041E\u0448\u0438\u0431\u043A\u0430 \u043F\u0440\u0438 \u043F\u0435\u0440\u0435\u043C\u0435\u0449\u0435\u043D\u0438\u0438: ${e.message}`);
    }
  }
};
